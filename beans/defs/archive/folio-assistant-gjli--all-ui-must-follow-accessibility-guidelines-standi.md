---
# folio-assistant-gjli
title: ALL UI must follow accessibility guidelines — standing rule, and the KG viewer has known gaps
status: completed
type: task
priority: normal
created_at: 2026-09-19T00:23:00Z
updated_at: 2026-09-22T06:54:25Z
parent: folio-assistant-o3xy
---


_2026-09-19T00:23:16Z_ — OWNER, 2026-09-19, verbatim: 'ALL UI MUST FOLLOW ACCESSIBATILITY GUIDELIENS'. Stated in caps, so a STANDING RULE rather than one task. It binds the KG viewer, the action-icon tiles (bean 1le7), the docs site and anything future. AGENTS.md says the discipline lives in skills/, so this needs a skill — provisionally folio-core/ui-accessibility — not just a bean, and the skill must be reachable from any activity that produces UI. WHY IT IS LOAD-BEARING HERE AND NOT BOILERPLATE: the owner has very limited hand function. .harness/interaction.json already records the low-dexterity profile and shapes every question this repo asks. A UI that needs precise pointing, or that cannot be driven from the keyboard, is unusable by the person it is built for. Target size and keyboard reachability are therefore not the low-priority end of WCAG here; they are the point. KNOWN GAPS IN THE VIEWER I JUST MERGED (#307), found by reading my own diff against this rule rather than by a tool: (1) the one-hop neighbourhood SVG uses <circle data-goto> with a click handler — NOT focusable, not a button, no keyboard path. A keyboard user can reach every edge in the table but none in the diagram. Worst gap. (2) selecting a node rewrites the detail panel with no aria-live region, so a screen reader is not told anything changed. (3) the search input has a placeholder and no label, so its accessible name comes from placeholder text that vanishes on input. (4) no skip link past the facet column to the results. (5) colour contrast of --dim on --bg and of the amber warn pair is UNMEASURED in both schemes. (6) focus visibility relies on the UA default; never checked against the accent backgrounds. (7) tile/target sizes unmeasured against WCAG 2.2 SC 2.5.8 (24x24 min). WHAT IT WOULD TAKE TO CHECK RATHER THAN ASSERT: axe-core via the existing Playwright setup gives automated coverage of contrast, names and roles; keyboard-path and target-size checks are e2e assertions (tab through, assert every interactive element reachable and activatable by Enter/Space). Nothing here is checkable today — no a11y gate exists in this repo.

_2026-09-19T00:53:03Z_ — DONE on branch claude/ui-accessibility, 2026-09-19. Three parts, and the middle one is the measured claim. (1) SKILL skills/folio-core/ui-accessibility.md — the rule needed a home, since a rule that lives only in a bean is one the next agent will not find. (2) THE VIEWER: all seven gaps fixed. SVG neighbours are now role=button groups with tabindex, aria-label naming target and edge, Enter/Space handlers and a 16px invisible hit disc; aria-live region on the detail panel plus a status line announcing match counts; a real visually-hidden label on the search field; a skip link on the first Tab; --on-accent/--on-accent-dim tokens per scheme with measured ratios; an explicit :focus-visible ring; 32px list/facet rows and 28px inline edge controls. (3) GATE tests/a11y.e2e.ts, 13 tests, wired into code-quality-gates.yml as a new e2e job — because bunx playwright test ran in NO workflow before, so 29 e2e tests existed and none was a gate. THE MEASUREMENT WORTH KEEPING: axe found 2 defects (contrast, target-size) and NONE of the 4 found by reading. It could not see the worst — a bare <circle> with an onclick is not RECOGNISED as interactive, so there is no control to fault. The converse also held: making them real buttons introduced nested-interactive (role=img is a leaf, now wrapping focusable children) and axe caught that where I had not. Neither method replaces the other; the skill says so. CONTRAST DEFECT WORTH REMEMBERING: color:#fff on --accent is 5.97:1 in light and 2.19:1 in dark, because dark mode's accent is a LIGHT green. One value written for one scheme, never rechecked in the other. TWO THINGS ONLY LOOKING CAUGHT: the flex change broke float:right so facet counts rendered before their labels ('1112 All'), fixed in the DOM rather than with CSS order so the spoken reading is right too; and three separate syntax breakages from backticks inside the page's template literal, now warned about in the function doc. NOT VERIFIED and stated in the skill: whether a screen reader announces what the markup implies, vestibular effects, colour-blindness beyond ratio.

_2026-09-22T06:51:22Z_ — CLOSED ON EVIDENCE, not on authorship, 2026-09-22 — `bean-coordination` §"Closing a bean whose work has already landed". This session did NOT do this work; the 2026-09-19 note above did, on branch `claude/ui-accessibility`. What this session did was establish the claim was still TRUE on today's `main`, because a bean left `in-progress` for three days with merged work behind it is indistinguishable from one that stalled.

WHY IT WAS TAKEABLE AT ALL. `gjli` carried a claim and no owner in the room, so liveness was checked before anything else: no unmerged branch touches `test/a11y.e2e.ts` or `skills/folio-core/ui-accessibility.md`, and no open PR names `gjli`. Eight quiet claims were swept the same way; this is one of them. `bean-coordination` §"A claim is branch-local" is why that check is not optional — a claim ANNOUNCES rather than reserves, so the way to tell an abandoned one from a live one is to look for the branch, not to trust the status field.

THE FOUR VERIFICATIONS, each against `origin/main` rather than the working tree:
1. `cat-harness/skills/folio-core/ui-accessibility.md` — present, 121 lines. The rule has the home AGENTS.md requires; it is not living in this bean.
2. `cat-harness/test/a11y.e2e.ts` — present. The note above says 13 tests; it now carries 21 `test(` literals, so others have added to it since.
3. `.github/workflows/code-quality-gates.yml` — the job "End-to-end + accessibility (hard)" runs `bunx playwright test` (line 1024). Observed `success` on ~8 heads across this session. The workflow's own comment states this session's thesis better than this bean does: *"a rule whose check does not run is an assertion. `test/a11y.e2e.ts` would have been decoration without this."*
4. Executed locally 2026-09-22 on `039175cf7`: **38 tests passed in 24.9s**, zero failures.

THE COUNT THAT WAS WRONG, AND WHY IT MATTERS HERE. 21 is the number of `test(` literals; 38 is the number that RUNS. Four of the suites are parameterised — `302:7` covers eight scheme×view combinations, `362:5` and `380:5` two schemes each, `565:7` four scheme×viewport pairs, `630:5` two — so reading the source undercounts the gate by 45%. This bean is about UI that is checked rather than asserted, and a count taken from the source is an assertion about the check. Take it from the run.

THE GAP THAT LOOKED LIVE AND IS NOT. `gen-library-viz.ts:278` emits `"<h3>" + esc(e.title) + "</h3>"` as raw HTML, which is the shape that caused the `preview:site` defect AGENTS.md records — 22 headings on one page sharing one anchor because kramdown assigns raw `<h3>` no id. Checked rather than assumed: the 12 remaining raw `<h3>` are all inside standalone `<!doctype html>` pages that kramdown never processes, spread 1–2 per page across 10 files, with no duplicate anchors among them. `gen-handler-index.ts:129` is a comment ABOUT a fixed earlier draft, not a live emitter. So the defect class exists in the generator and the instances do not; this is not the bean that fixes that, and inventing work to justify closing one is worse than closing it.

WHAT IS STILL NOT VERIFIED, carried forward rather than closed silently — the same three the 2026-09-19 note listed, none of which a gate can settle: whether a screen reader ANNOUNCES what the markup implies, vestibular effects, and colour-blindness beyond contrast ratio. They are stated in the skill, which is where a standing rule's open edges belong. The STANDING RULE does not close with this bean: `gjli` tracked giving the rule a home and a gate, and both exist. The rule itself binds every future UI and is now discoverable by an agent that asks for the skill.


_2026-09-22T07:02:00Z_ — THIS BEAN'S BODY WAS RECONSTRUCTED, and a reader is owed that plainly. The closing note above was written with `beans update --body-file`, which **replaces** the body rather than appending to it: the 2026-09-19 OWNER note and the 2026-09-19 DONE record were both deleted, exit 0, no warning. They were recovered from `git show HEAD:<this file>` and reassembled under the new front matter within the same session, so no commit ever carried the truncated version. The two 2026-09-19 notes are therefore byte-identical to what was committed on 2026-09-19; only their position relative to this note is new. Recovered from **git rather than from the session transcript**, which also held them — a transcript is a copy the agent made, and only the repository is checkable by anyone else. Full finding and the skill fix: bean `i9xp`, and `todo-manager` §"Check before you UPDATE".
