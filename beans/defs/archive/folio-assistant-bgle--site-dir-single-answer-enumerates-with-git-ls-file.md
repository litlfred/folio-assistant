---
# folio-assistant-bgle
title: site-dir-single-answer enumerates with git ls-files, so a NEW file is invisible to it until committed
status: completed
type: task
priority: normal
created_at: 2026-09-19T11:09:58Z
updated_at: 2026-09-19T12:02:17Z
parent: folio-assistant-1xhc
---


_2026-09-19T11:09:58Z_ — Measured 2026-09-19 on branch claude/wonderful-bohr-6kxh7b while building bean iurf. sourceFiles() in scripts/tests/site-dir-single-answer.test.ts enumerates via 'git ls-files', which lists TRACKED files only. Two new files of mine (scripts/gen-themes-css.ts, scripts/tests/theme-tokens.test.ts) both hardcoded the site root. A full 'bun test' run before committing reported 0 fail — the gate could not see them. The same suite on CI, after the commit, reported the failure. So the gate is blind to precisely the files most likely to violate it, at precisely the moment their author would catch it, and CI is the earliest possible detection. Not a hypothetical: it cost one red CI cycle on PR #405. Fix is probably to union 'git ls-files' with the untracked-but-not-ignored set ('git ls-files --others --exclude-standard'), which keeps node_modules and build output out while seeing new work. Worth checking whether any sibling gate enumerates the same way and has the same blind spot.


_2026-09-19_ — DONE, and the bean's own "worth checking whether any sibling gate enumerates the same way" was worth checking — but the answer is SMALLER than I had been saying out loud. I called this systematic; it is TWO. Four call sites of `git ls-files` in .ts: (1) scripts/tests/site-dir-single-answer.test.ts — the measured defect; (2) scripts/tests/workflow-repo-name.test.ts — SAME BLIND SPOT, not previously noticed, and a brand-new workflow hardcoding the repo name is exactly when that guard is worth having; (3) scripts/tests/lean-projects.test.ts — uses `--cached` DELIBERATELY, asking whether .lake/ is tracked, so tracked-only is the correct question and changing it would break it; (4) scripts/scan-repo-content.ts — already distinguishes the two enumerations with a documented flag ("whether the tree came from git ls-files or a filesystem walk"). So: 2 fixed, 1 correct as-is, 1 already aware. Recording the distinction in the module docs matters more than the fix — the next person doing a sweep would otherwise "fix" lean-projects and break it. FIX: scripts/repo-files.ts, `repoFiles` / `repoFilesWithExt`, unioning `ls-files` with `ls-files --others --exclude-standard` exactly as the bean proposed, plus two things the bean did not mention: ignored files stay out (--exclude-standard is load-bearing; without it the set includes node_modules and the gate gets abandoned rather than fixed), and a tracked-but-DELETED file is filtered, because ls-files still lists one and a gate reading it would fail on content nobody can fix from a checkout. FALSIFIED BEFORE TRUSTED: dropped an untracked scripts/__bgle-violator.ts hardcoding docs/folio-assistant/... — site-dir gate went RED (1 fail), removed it, GREEN (3 pass). That is the #405 scenario reproduced and now caught BEFORE the commit instead of by CI after it. The test suite asserts the property with a REAL untracked file rather than a mocked listing, because the defect was in WHAT GIT WAS ASKED, so a test that stubs git would have passed against the broken version. Pleasing recursion: repo-files.ts was itself untracked while the gate ran over it — the fix checked itself. Verified: 6 new tests pass, full suite 2552 pass 0 fail, tsc clean, eslint clean, kg:audit:check rc=0.
