---
# folio-assistant-x3bd
title: Top-level topical KG directories, bootstrap/ first
status: in-progress
type: task
priority: normal
created_at: 2026-09-19T05:59:28Z
updated_at: 2026-09-20T04:05:17Z
parent: folio-assistant-vke6
---


_2026-09-19T05:59:49Z_ — THE ASK (owner, 2026-09-19, answering where the harness schemas should move): 'toplevel topical directories like: bootstrap =/ KG w/ only tool the CLI fallback for beans. minimal workflow, init, absolutely minimal skills and bpms listt to get going.' So the organising axis is TOPIC, not layer — which also settles the earlier layer question by construction, since math/ and who-smart/ are core and sci while bootstrap/, coordination/ and crdm/ are harness, and the layer becomes readable from the path with no triage list. TWO MEASUREMENTS MAKE THIS CHEAP, both taken 2026-09-19 on 6c1550e0. (1) BPMN skill refs are by BARE NAME: 47 distinct <folio:skill ref=...> across processes/, ZERO containing a slash. So moving a skill between directories breaks no reference. (2) gen-skill-docs publishes FLAT by name — docs/reference/skill-instructions/ has zero subdirectories — so moving a skill changes no published URL. The cost is therefore entirely in DISCOVERY CODE, and discovery was centralised the same night: known-skills.ts is the single definition and kg-export imports it rather than copying it (#322). One change point, not 148. THE ONE REAL COUPLING: the BPMN loader hardcodes processes in two production places, src/tools/workflow.ts:46 (WORKFLOW_SRC) and src/impact/stakeholder-map.ts:99 (wfDir). CURRENT STATE: 34 BPMN + 4 DMN all in one processes/; 148 skill .md across six packages of which folio-core alone is 75 — a grab bag holding bean-blocking, bib-photo-ingestion-watcher, crdm-detect, html-rendering-qc, milnor-exposition-standard and ui-accessibility side by side, which is exactly what topical directories fix. BOOTSTRAP/ DEFINED, drawn from the real corpus: tools beans-manual (scripts/beans-fallback.ts), beans-cli, folio_init; skills directory-conventions (nothing else resolves without it), interaction-modality (the session sweep emits it FIRST because it changes the form of every question), getting-started, todo-manager, bean-coordination, session-intent, process-state; BPMN getting-started.bpmn and bean-lifecycle.bpmn; schemas cat-harness.ts, bean-graph.ts, kg-node.ts, namespaces.ts, tool.ts, tool-types.ts. Declared as one entry { id: bootstrap, path: bootstrap/, graphs: [cat-harness, tools, schemas] } — a directory holding more than one kind is already supported (schemas/ does it) and the files self-declare, which #326 made true for schemas. THE TEST OF BOOTSTRAP IS FALSIFIABLE: a checkout with ONLY bootstrap/ and cat-harness.json must let an agent claim a bean, read the conventions and run folio_init. If it cannot, the list is wrong. Worth a CI job. THE REST, SKETCHED: authoring/, review/, publishing/, evidence/, crdm/ (its 6 BPMN already cluster), coordination/ (the watcher family), quality/, math/ (today's folio-paper-adapter + authoring-math), who-smart/ (today's authoring-who-smart-guidelines). ORDER OF OPERATIONS: (1) make discovery declaration-driven — known-skills.ts and the BPMN loader read the declared directories instead of the literals skills/ and processes/; NO FILES MOVE, ships green, and everything after is a file move. (2) Wire resolveSkillDirs in schemas/harness-config.ts, which AGENTS.md records as computing the cross-instance skill overlay and having NO CALLER — so discovery is root-only today and this design needs it; a named Phase 0.1 prerequisite, not new work. (3) Create bootstrap/ and move its ~15 files, verified by the bootstrap-only CI job. (4) Then one topical directory per PR, each independently revertible. RISK: step 1 is the only risky one; steps 3-4 are moves whose references provably do not care. NOT IN SCOPE and separate questions: .claude/skills/local/ (23 json + 3 md) sits outside this, and the 188-line todo-manager divergence AGENTS.md already flags. STATUS: plan only, nothing implemented. Awaiting the owner's go on step 1 and on whether to commit this as docs/proposals/topical-kg-directories.md.

_2026-09-19T06:14:13Z_ — CORRECTION to this bean's own figures: the corpus is 30 BPMN + 4 DMN = 34 diagrams TOTAL, not '34 BPMN + 4 DMN'. I read a 34-line ls that already included the decisions/*.dmn and reported the total as the BPMN count. Caught by workflowFiles() returning 34 where I had predicted 38. STEP 1a SHIPPED (commit 613f92987): DEFAULT_DIRECTORIES in the container schema, seeded into resolveDirectories as the outermost link of the chain and overridden by id like any other link; existence-filtered because a defaulted-but-absent directory is the dh4f defect in every instance at once, while an EXPLICIT entry is honoured either way (the instance asserting something vs the platform guessing). An instance whose declaration is {"name":"minimal"} now resolves exactly the conventional directories it actually has. STEP 1b IN PROGRESS: skillMdDirs() now reads the DECLARATION rather than the literal skills/ — and one rule had to be found by measurement rather than reasoning. Including any directory whose graphs contain cat-harness pulled in schemas/README.md and schemas/block-qa-schema/README.md, giving 150 skills where the corpus has 149, because schemas/ declares ['schemas','cat-harness'] (a schema IS a KG node) while its .md are READMEs and its nodes are .ts. Requiring YAML front matter would have been the principled rule and is MEASURABLY WRONG: only 117 of 147 skill bodies carry any, so it would have dropped 30 real skills. The rule that holds: a directory declared for EXACTLY ['cat-harness'] contributes skills; one declaring additional kinds holds several things and has to say which file is which, which for schemas/ is @graphNode on the .ts. Back to 149 and the same eight directories, now read from the declaration. PROVED: a scratch instance declaring only bootstrap/ and crdm/ discovers both and finds their skills with ZERO code changes, which is the deliverable of step 1. workflowDirs()/workflowFiles() added for the BPMN side and verified against both layouts (processes here, bootstrap/workflows there). REMAINING in 1b: rewire the nine production sites that hardcode processes — src/tools/workflow.ts:46, src/impact/stakeholder-map.ts:99, src/workflow/corpus-gate.ts:45, scripts/kg-audit.ts:96, scripts/translate-bpmn.ts:39, scripts/render-bpmn.ts:24, scripts/xml-comment-check.ts:115 — and add the gate refusing a new hardcoded path, which is the least-maintenance half: without it site #18 appears and nobody notices, which is how three readers of 'where do skills live' drifted apart before #322.

_2026-09-19T06:20:34Z_ — BOOTSTRAP REDEFINED by the owner, 2026-09-19, replacing the seven-skill list earlier in this bean: 'dont need all markdown skills in bootstrap/ only skill is to read markdwon from KG (in json or jsonld format) and what it means in terms of the graph. bootstreap skills include in self descriptin the BRIEFEST of desction of the core data model, bpmn flow, and the first step (not all steps) to start load dependency tree of whatever instance they are in, if they are in one, or the first step to initialize one if not'. So bootstrap ships the ABILITY TO READ THE GRAPH plus exactly one next step, and everything else is discovered from the graph. That is strictly better than the seven-skill list I proposed, which would have grown into a second copy of the corpus and then drifted from it. The single skill's self-description carries: (a) the briefest statement of the core data model, (b) the BPMN flow, (c) the FIRST STEP ONLY — begin loading the dependency tree of whatever instance you are in, or initialise one if you are not in one. TWO PREREQUISITES FOUND BY MEASUREMENT, both blocking this design, measured on 21a1379ce. (1) A SKILL BODY IS NOT REACHABLE FROM THE PUBLISHED GRAPH. A Skill node carries instructions='skills/folio-core/getting-started.md', a REPO-RELATIVE PATH, and hasInstructions=true, a boolean. No field gives a dereferenceable URL. The body IS published, at docs/reference/skill-instructions/<name>.html, and the graph never names that URL. So a consumer holding only the JSON-LD cannot fetch a skill body — which is the whole job of the one skill bootstrap is supposed to contain. Same defect class as the /kg/ literal fixed earlier tonight: a link-shaped value that does not dereference. Contrast inputSchema/outputSchema, which ARE minted as published IRIs by skillIoIri. (2) NEITHER instructions NOR hasInstructions IS IN @context, so a JSON-LD processor DROPS BOTH — they are among the 34 undeclared terms of bean ovkk. In JSON-LD form the path is not merely unfollowable, it is absent. CONSEQUENCE: 'read markdown from the KG in json or jsonld format' cannot work today in JSON-LD at all, and works in plain JSON only with the repository checked out. So before bootstrap/ can exist, the graph must publish a dereferenceable instructions URL (the docs-site page, or the raw .md, decided deliberately) and declare the term in @context as a link. That is a small, self-contained change and it is the real step 3a — ahead of creating the directory, because creating bootstrap/ first would ship a skill whose own premise is false.

_2026-09-19T06:20:54Z_ — OWNER, same exchange: 'the content should be in README.md (so self documenting in github, too). README.md brief!!!!' So the bootstrap skill's self-description lives in bootstrap/README.md, not in a <skill>.md beside it. Three things that buys, and the third is the one that matters. (a) GitHub renders a README in the directory view, so somebody who lands in the repository with no tooling and no checkout sees the core data model, the BPMN flow and the first step immediately — no MCP, no skill_fetch, no JSON-LD processor. (b) It is the same file the docs site and any blob URL already serve, so it needs no new publishing path. (c) It SIDESTEPS prerequisite (1) above for humans entirely: the 'skill body is not reachable from the published graph' defect bites a consumer holding only the JSON-LD, and a README is reachable by every other route. The graph should still NAME it — the term still has to be declared in @context and given a dereferenceable URL — but the content is no longer hostage to that being done first. BRIEF IS A HARD CONSTRAINT, stated emphatically ('README.md brief!!!!'). It is also the constraint most likely to erode: every future session will have a reason to add one more paragraph, and AGENTS.md is itself the worked example of a bootstrap pointer that grew into a reference. Worth a measured budget rather than an intention — kg-audit already has skill-is-brief as a criterion, and pointing it at bootstrap/README.md would make the limit enforced rather than hoped for. NOTE ALSO: the README must be AUTHORED prose, not generated. content/pipeline/readme-sections.ts writes only inside <!-- marker:begin/end --> pairs and touches nothing outside them, so a generated section could be added later without endangering the authored text — but the brief description itself is prose a generator would have to invent, which is exactly the argument AGENTS.md records for deleting scripts/generate-readme.sh.

_2026-09-19T06:33:59Z_ — BOOTSTRAP IS A STANDALONE GRAPH, AND cat-harness IS AN INSTANCE OF IT — the owner's inversion, 2026-09-19: 'can we actually make cat-harness an instance of bootstrap/ which then declares goes on to read in cat-harness's assets. so bootstrap is super lean, referencing only the minimum of schema' ... 'so bootstrap is a stnadalone graph.' That is a strictly different shape from the one sketched earlier in this bean, where bootstrap/ was one more topical directory ALONGSIDE the others inside this repo. Here bootstrap is the BASE: it declares nothing about this repo, and this repo's cat-harness graph is just the first instance bootstrap happens to load. The topical directories then become instance content rather than peers of bootstrap. NOTE THE DIRECTION RULE IS PRESERVED AND TIGHTENED: harness may not import core, and now bootstrap may not import harness either. MEASURED, on 652a79157, by transitive relative-import closure from each entry point (the script resolves ./x.js to ./x.ts and follows only relative specifiers). schemas/cat-harness.ts -> 3 modules: cat-harness.ts, kg-node.ts, namespaces.ts. schemas/bean-graph.ts -> 4 modules: those plus itself. schemas/harness-config.ts -> 7 modules: those 3 plus block-kinds.ts, block-qa.ts, contributions.ts, harness-config.ts. SO THE READING HALF IS ALREADY LEAN AND THE WALKING HALF IS NOT, AND THE REASON IS CO-LOCATION, NOT DEPTH. Everything needed to READ a declaration and resolve its directories is 3 modules. Everything needed to WALK A DEPENDENCY TREE — declarationChain() and flattenDependencies(), which is precisely the 'first step' the owner wants bootstrap to carry — costs 7, because those two functions live in harness-config.ts beside loadContributions(), and it is loadContributions that imports ContributionRegistry and through it block-kinds and block-qa. Verified: across the 190-line span holding flattenDependencies, declarationChain, materialiseDeclaredDirectories and resolveSkillDirs, the identifiers Contribution / BlockKind / BlockQa appear ZERO times outside one section-header comment. The content model is not a dependency of the dependency walk; it is a neighbour. CONSEQUENCE, and it is the cheap kind: bootstrap needs no new abstraction and no re-implementation of the walk. It needs the walk SPLIT OUT of harness-config.ts into a module whose own closure is the same 3, leaving harness-config.ts as the content-model config that imports it. Bootstrap's minimum schema surface is then 4 files (namespaces, kg-node, cat-harness, the chain walker), plus bean-graph only if bootstrap carries the beans fallback. That is the answer to 'referencing only the minimum of schema': 4, not 7, and the 3 extra were never conceptually part of it. FALSIFIER FOR THE SPLIT, so it is not merely tidying: afterwards a checkout containing ONLY bootstrap/ and those 4 schema files must resolve its own declaration and walk to a dependency, importing nothing under content/ or schemas/block-*. If it cannot, the split line is in the wrong place. STILL BLOCKING, unchanged: the published graph gives no dereferenceable URL for a skill body, and neither instructions nor hasInstructions is in @context. The README route sidesteps that for humans; it does not sidestep it for the JSON-LD consumer, which is the reader bootstrap's one skill exists FOR.

_2026-09-19T06:34:26Z_ — bootstrap/README.md — CONTENT SPEC from the owner, 2026-09-19, same exchange: 'and instructions in README refer to self-documention json/json-ld and just give the briefest funcional actor/role process flow, task skill, read markdown/navigate the KG, user input, tool overview. one user story, bpmn is decision tree (initialize or load w/ request user intput)'. This settles what BRIEF means, which the previous note flagged as the constraint most likely to erode. It is not a word budget applied to arbitrary prose; it is a FIXED SHORT LIST OF SECTIONS, and anything not on the list is out. THE README POINTS, IT DOES NOT RESTATE. 'instructions refer to self-documentation json/json-ld' means the README names the published renderings and lets the graph describe itself, rather than carrying a second description of the model that can drift from it. That is the same argument AGENTS.md makes against itself in its own opening banner: a bootstrap pointer that restates the discipline becomes a rule with no home. It also makes the renderings load-bearing rather than decorative — they were moved to the publish base earlier tonight (they had been under /kg/ only), so the README can name a URL that resolves. THE SECTIONS, and this is the whole file: (1) the functional flow in its briefest form — actor / role / process / task / skill, which is the single sentence AGENTS.md already carries ('an actor performs a task in a process as a role, using that role's skills') and needs no expansion here; (2) read markdown / navigate the KG — the one skill bootstrap ships; (3) user input — where a human is asked, which is a real lane, not a footnote; (4) tool overview — what tools exist, not how each works; (5) ONE user story, singular; (6) the pointer to the self-describing json / json-ld. THE BPMN IS A DECISION TREE, NOT A PIPELINE. One gateway, and this is the structural point: AM I IN AN INSTANCE? Yes -> load its dependency tree. No -> initialize one. Either branch may request user input. That is materially smaller than getting-started.bpmn or bean-lifecycle.bpmn, the two diagrams the earlier seven-skill list in this bean proposed to ship, and it matches the owner's earlier 'the FIRST STEP (not all steps)': a decision plus one action per branch has no second step to get wrong. THREE THINGS THAT FALL OUT, worth recording before anyone writes the file. (a) The request-user-input branch needs a lane for the human, so the role model applies from the first diagram; scenarios already has the vocabulary and `judgementOnly` is the flag for a lane that acts but has no procedure. (b) 'Initialize' is folio_init, which AGENTS.md records as registered among the GENERIC tools precisely because it runs before a folio has a content type — so the no-instance branch already has its tool and bootstrap does not have to invent one. (c) The 'tool overview' section is the one section at risk of becoming a generated table; it should name tools and stop, because an overview that lists every tool is the corpus copy this bean already rejected once.

_2026-09-19T06:36:26Z_ — README IS A LINK DENSITY CONSTRAINT, NOT A LENGTH ONE — owner, 2026-09-19: 'in readme you dont need to say what a tool is, just a hyperlink to he json schema defn or so. same for all the terms. almost every word in the readme should be linked to a definition.' This is what actually enforces BRIEF, and it is far better than a word budget, because a word budget is a number somebody argues with and this is a rule that makes padding IMPOSSIBLE: you cannot write an extra paragraph about what a Tool is, because the paragraph's job is done by the link and there is nowhere to put the prose. It also makes the README a CONFORMANCE TEST OF THE GRAPH rather than documentation of it — a term with no URL to link to is a term the graph has not defined, and the README is where that shows up.

AND THAT TEST FAILS TODAY, FOR EVERY TERM. MEASURED on 7b33b1a11 from the real export (_kg/folio-assistant.jsonld, 1185 nodes): the graph uses 12 classes as @type — Actor, Capability, Directory, GraphKind, Process, ProcessNode, Role, Schema, SequenceFlow, Skill, SkillPackage, Tool — and maps 28 properties into the folio namespace via @context. 41 distinct terms, ALL under FOLIO_NS = https://litlfred.github.io/folio-assistant/ns#. NOTHING SERVES THAT STEM. This is not an inference: scripts/tests/kg-export.test.ts:332 exempts it BY NAME from the dead-link check, with the comment 'The term namespace is an IRI stem, not a file — nothing serves it and nothing should try to.' That was the right call while a term only had to be an IDENTIFIER. The owner's requirement changes it to a DEFINITION, and a definition has to dereference. So every term link in the README would 404 on the day it is written. (Could not be confirmed against the live site from this container — the egress proxy refuses litlfred.github.io — so this rests on the repository's own statement of what it publishes, which is stronger evidence here anyway: the test says nothing serves it BY DESIGN.)

THIRD INSTANCE OF ONE DEFECT CLASS, and worth naming as such because it will recur: a link-shaped value that does not dereference. (1) toolTypeIri hardcoded /kg/ after the renderings moved — 95 published refs would have 404'd. (2) A Skill node's `instructions` is a repo-relative path and `hasInstructions` is a boolean, so a consumer holding only the JSON-LD cannot fetch a skill body; neither term is in @context, so a processor drops both. (3) This: 41 terms in a namespace nothing serves. Each was found by asking whether a self-URL actually resolves, and none by any test that was watching.

SO THE README'S REAL PREREQUISITE IS PUBLISHING /ns. Small and self-contained: a vocabulary document with an anchor per term (an HTML page, a JSON-LD/SKOS vocab, or both at one URL by content negotiation), generated from the same source the @context and the @type IRIs come from so the three cannot disagree — the terms are already enumerable, which is how the 41 above were counted. It also retires the by-name exemption in kg-export.test.ts: once /ns is a document, the dead-link check should cover it rather than skip it, and THAT is the regression guard that stops instance four of this defect class.

SEPARATELY, ovkk IS NOW ON THE CRITICAL PATH rather than being tidy-up. 34 property names, 3583 occurrences, are used in @graph and absent from @context — a JSON-LD processor drops every one. A README whose premise is 'the json/json-ld is self-documenting' cannot ship while a third of the graph's vocabulary vanishes on load.

UI AND TRANSLATION ARE NOT BOOTSTRAP — owner, same exchange: 'ui stuff like translations (skills, tooling) are not in bootstrap, it is in cat-harness/'. Consistent with bootstrap-as-base: bootstrap ships the ability to read the graph and one next step, and a presentation layer is something an instance has, not something the reader of a declaration needs. The cluster this names is real and already clustered — skills/folio-core/translation-manager.md, processes/translation-workflow.bpmn and human-translation-workflow.bpmn with their two kg-qa sidecars — so it is a candidate topical directory inside cat-harness, not a bootstrap concern. NOTE THE CONSEQUENCE FOR THE README ITSELF: bootstrap/README.md is therefore mono-lingual by design, and translating it is a cat-harness capability applied to bootstrap from outside, never a second file inside bootstrap/. If that is wrong the place to say so is here, before the directory exists.

_2026-09-19T06:40:18Z_ — USER STORY: YES, IT ALREADY HAS A SCHEMA, AND IT IS SMALLER THAN ANYTHING I WOULD HAVE DESIGNED. Owner asked, 2026-09-19: 'does userstory already have a scheme? see whats in smart-base if not. model smallest thing a userstory involving roles, should refer to processes, tasks and/or i/o needs for a skill' and then 'how small can we make it?'. Checked this repo first: NOTHING — one incidental mention in bean p2en and no schema, no node kind, no block kind. Then cloned WorldHealthOrganization/smart-base (shallow, 2026-09-19) and it is there.

THE SCHEMA IS input/fsh/extensions/SGUserStory.fsh, AND IT HAS TWO FIELDS. Verbatim: Extension SGUserStory, 'Smart Guidelines extension to support structured User Stories (As a Actor I want to capability so that benefit)', with `extension contains SGString named capability 1..1` and `SGString named benefit 1..1`. That is the whole definition. THE ANSWER TO 'HOW SMALL' IS THEREFORE TWO, AND THE REASON IS THE INTERESTING PART: it is an EXTENSION, not a resource. It does not name an actor because it is ATTACHED to something that already has one. Attaching instead of declaring is what bought the third field back, and it is the move I would have missed — I was heading for id + role + want + refersTo and calling four small.

WHAT IT ATTACHES TO IS SGRequirements, WHICH IS ALREADY THE WHOLE MODEL THE OWNER DESCRIBED. input/resources/StructureDefinition-SGRequirements.json profiles FHIR Requirements and adds exactly three extensions: SGTask, Satisfies, SGUserStory. Base FHIR Requirements supplies actor (0..* canonical to ActorDefinition) and statement (key, label, conformance, requirement, satisfiedBy). So: role = Requirements.actor; task = the SGTask extension; satisfies = the Satisfies extension; the story = capability + benefit. 'A user story involving roles, referring to processes, tasks and/or I/O needs for a skill' is that resource, and it needed no new type for any of it.

AND WE HAVE INDEPENDENTLY REBUILT MOST OF IT ALREADY, WITHOUT THE LABEL. skills/requirements/*.json in this repo — seven files, agent-workflow.json at the root of the lattice — carry { id, title, description, actors[], statements[{ key, label, conformance, requirement, satisfiedBy[{kind, ref}] }] }. That is FHIR Requirements field for field, conformance codes and all, arrived at separately. WHAT WE LACK IS EXACTLY THE TWO EXTENSIONS: no task, and no user story. So adding user stories here is NOT a new node kind, a new directory, a builder, a viewer or a QA family — it is TWO OPTIONAL FIELDS ON A SCHEMA THAT ALREADY EXISTS: userStory: { capability, benefit }. Everything the owner asked it to refer to is already reachable from the Requirement it hangs on.

SMART-BASE ALSO MODELS SKILLS AS ACTORS, which is worth knowing before we diverge from it. input/fsh/actors/SGAuthoring.Skills.*.fsh are Instances of SGRequirements — ReviewAndApproveContent, L2Authoring, L3Authoring, ContentReview, Translation, Publication, ProjectManagement — each with actor pointing at an SGAuthoring.Persona.*, six extension[task] codings, one extension[userstory] with its capability and benefit, and six or so numbered statements (APPROVE-01 …). So in smart-base a SKILL IS A REQUIREMENTS INSTANCE: 'can review and formally approve content' is a capability statement about a persona, not an instruction body. OUR Skill is the instruction body and our Role carries skills[]. These are compatible but NOT the same node, and writing an alignment note is cheaper now than discovering the mismatch during an import.

BRIEF DEFINITIONS IN THE SCHEMA, RICH ONES IN cat-harness/docs — owner, same message. Note that smart-base already works this way and is the worked example: the FSH Description of SGUserStory is one sentence containing the As/I want/So that template and nothing else, and the interpretation lives in the IG's page content. Also note the shape to copy from UserScenario.fsh, which solves the same brief-vs-rich problem inside one field: `description[x] 1..1 string or uri` — either the markdown inline OR a URI to a markdown file relative to the repository root. That is precisely the 'brief in the schema, rich in the docs' rule made structural rather than editorial, and it is a pattern worth lifting.

NOT DECIDED, and it is the one real question: whether we adopt SGUserStory's IRI (http://smart.who.int/base/StructureDefinition/SGUserStory) or mint our own term under FOLIO_NS. Adopting costs nothing today and makes a DAK folio's stories the same objects as ours; minting keeps the harness free of a WHO dependency, which is the direction rule this repo enforces everywhere else. Deferred deliberately — it is one line in @context either way, and it wants the owner.

_2026-09-19T06:48:18Z_ — README'S CLOSING SECTION — THREE STEPS, ORDERED, owner 2026-09-19: 'in README once given functional explanation invoving all the terms, it should tell the reader they shoud: point the reader to .md containing skill to load, navigate and read documentation in a KG; point the read to the bootstrap jsonld and have them load the graph and read the bootstrap process; start the bootstrap bpmn process.' So the file's shape is now fully specified: the linked functional explanation first (actor/role/process/task/skill, reading the KG, user input, tool overview, one user story, every term a link), then a THREE-LINE HANDOFF, then nothing. That is the end of the README and the beginning of everything else.

THE ORDERING IS THE DESIGN, NOT THE FORMATTING. Step 1 hands over a .md — a skill body a reader can read with no tooling at all. Step 2 hands over a JSON-LD graph, which the reader can now navigate BECAUSE OF STEP 1. Step 3 starts a process the reader found IN that graph in step 2. Each step is usable only after the previous one, and step 1 needs nothing. THE CONSEQUENCE, AND IT IS EASY TO BREAK: the README and step 1 must both be readable WITHOUT the ability to navigate the graph, because acquiring that ability is what step 1 IS. A future edit that makes the README's terms resolvable only through the graph, or makes the skill body reachable only via skill_fetch, closes the loop and bootstrap stops bootstrapping. Worth stating as the falsifier: A COLD READER WITH A BROWSER AND NO TOOLING MUST GET THROUGH STEP 1.

THREE THINGS THIS MAKES CONCRETE THAT WERE VAGUE BEFORE.

(1) BOOTSTRAP NEEDS ITS OWN JSON-LD EXPORT. 'point the reader to the bootstrap jsonld' is a different artefact from _kg/folio-assistant.jsonld (1185 nodes, this whole instance). Bootstrap is a standalone graph, so it exports itself — small, its own @id, and it must be loadable WITHOUT the instance it is about to load. That is also the falsifier for bootstrap's leanness restated as an artefact: if bootstrap.jsonld cannot be produced from bootstrap/ alone, bootstrap is not standalone. kg-export already takes a root, so this is plausibly a parameter rather than a second exporter — check before writing one.

(2) STEP 1 NEEDS A REAL URL FOR A .md, WHICH IS PREREQUISITE (1) FROM TWO NOTES AGO, NOW UNAVOIDABLE. Previously the README sidestepped the undereferenceable-skill-body defect by carrying its content inline; this step does not sidestep it, it IS it — the README's job here is to hand over a link to a skill body. For a human the blob URL works today (AGENTS.md's readme.linkStyle default is blob precisely so a private repo's links follow the viewer's session). For the JSON-LD consumer reached in step 2 it still does not: a Skill node's `instructions` is a repo-relative path and is not in @context, so a processor drops it. So the two audiences need the same target published two ways, and that is a single decision to make once rather than twice.

(3) STEP 3 HAS TWO READERS AND ONLY ONE OF THEM HAS MCP. 'Start the bootstrap bpmn process' is workflow_start for an MCP-connected agent and is 'read the .bpmn the graph pointed you at, and do its first step' for anybody else — a cold agent, a human, a different toolchain. The README must state it in a way that is true for both, which argues for naming the PROCESS and its first step rather than naming a tool call. This is the same reason the diagram is a decision tree with one gateway: a first step you can perform by reading is a first step that does not require the thing you have not loaded yet.

NOTHING HERE CHANGES THE BREVITY CONSTRAINT. Three steps, three links, three lines. The temptation this creates is to explain each step, and the link-density rule already forbids it: the skill body explains itself, the graph describes itself, and the BPMN is the process.

_2026-09-19T06:49:45Z_ — BOOTSTRAP HAS TWO SKILLS, NOT ONE — owner 2026-09-19: 'need one skill, determine user intent in bootstrap propces'. Added to the one already specified ('only skill is to read markdwon from KG ... and what it means in terms of the graph'). So the pair is: READ THE GRAPH, and KNOW WHAT YOU ARE BEING ASKED TO DO WITH IT. Those are the two halves of a first step and neither works alone — a reader who can navigate the graph and does not know which branch to take is stuck at the gateway, and a reader who knows the intent and cannot read the graph cannot act on it.

IT IS THE GATEWAY'S SKILL. The BPMN the owner specified is one decision — am I in an instance? load its dependency tree, or initialise one — 'w/ request user intput'. This skill is what that gateway runs. Note what is NOT intent: whether a cat-harness.json exists is a FILESYSTEM FACT, mechanical, and needs no skill. What needs one is the part a file cannot answer: what does this person want done. The two are easy to conflate and conflating them would make the skill pointless, because the mechanical half would answer first and always.

AND THE THIRD STATE IS STRUCTURAL HERE FOR ONCE. This repository says everywhere that 'could not determine' is never rendered as a pass — readme-sections returns skip, ci-health never renders unreadable as green, the TOC reports an unreadable publish ref rather than blanking. In every one of those it is a discipline somebody has to keep. HERE IT IS A BRANCH OF THE DIAGRAM: intent not determined IS the request-user-input path, so failing to determine cannot silently become 'assume initialise'. That is the strongest form of the rule this repo has managed to state, and it is worth saying so in the skill body, because it is the reason the skill exists rather than a default.

THREE EXISTING SKILLS LOOK LIKE THIS ONE AND NONE OF THEM IS IT. Measured 2026-09-19, by reading them. (1) session-intent.md, 205 lines, is a durable-handoff coordination protocol over STATUS.md, docs/coordination/<goal>.md master ledgers, flip-flop history and the beans queue. It presupposes an instance with the whole coordination apparatus already loaded — precisely what bootstrap has NOT loaded yet. Reusing it would invert the dependency. (2) crdm-detect.md, 123 lines, classifies a request as feature work versus content work and routes it into the CRDM process; closer in spirit, still instance-dependent, and it answers a question that only arises AFTER you are in an instance. (3) interaction-modality.md, 288 lines, governs HOW to ask (context before the question, selectable options, the modality read from .harness/interaction.json) — it is the etiquette of the request-user-input branch, not the determination. So the bootstrap skill is NEW, and it is small: it decides between two branches and knows when to ask. That it has three near-neighbours is a reason to name it carefully and to say in its body what it is NOT, or a later session will 'consolidate' it into session-intent and take the dependency inversion with it.

SIZE CHECK, since the whole point is leanness. Two skills, one BPMN with one gateway, one README. The three near-neighbours total 616 lines and none is loadable at bootstrap time; the two bootstrap skills should be a small fraction of that, and if the intent skill starts approaching crdm-detect's 123 lines it has probably absorbed instance knowledge it should be deferring to the graph for.

PR STATE, unrelated but recorded so the next session does not re-derive it: #334's merge conflict is resolved (main merged, isSkillMd and declaration-driven discovery composed), 8c88b88f8 is fully green on all six hard checks, and mergeable_state is now 'unstable' — CI in progress on a later beans-only commit, NOT a conflict.

_2026-09-19T06:50:56Z_ — HARDCODING JSON/JSON-LD ASSETS IN BOOTSTRAP IS PERMITTED, NOT PREFERRED — owner 2026-09-19: 'if need to hardcode json/jsonld assets, thats ok. not preferrred'. Recorded with a fence, because this repository has spent the last two days removing exactly this: the /kg/ literal in toolTypeIri that would have 404'd 95 published refs, nine production sites hardcoding processes, five hardcoded skill directories that had already gone stale, and a generate-readme.sh holding one folio's title and badges. A blanket concession would re-open the class on the day it was closed. The fence below is not stricter than the owner said; it is the distinction that makes the concession safe to use.

THERE IS EXACTLY ONE HARDCODE THAT IS NOT A COMPROMISE AT ALL, AND BOOTSTRAP IS WHERE IT LIVES. You cannot discover the graph you use to discover things. bootstrap/README.md must name bootstrap.jsonld literally, because there is no prior graph to read that name from, and the same is true of the @context the file loads with. That is the BASE CASE OF THE RECURSION, not debt — it is permanent, it is correct, and marking it as a compromise would be wrong. Every bootstrap in every system has one; the failure mode is having more than one.

EVERYTHING ELSE IS A STAND-IN FOR A LOOKUP, AND THE TEST IS ONE QUESTION: COULD A GRAPH ALREADY LOADED HAVE ANSWERED THIS? If yes, the hardcode is debt, permitted but marked. Candidates that will come up: the term namespace (FOLIO_NS is a constant in schemas/namespaces.ts and is the RIGHT shape — one definition, imported, not retyped); the skill-instructions URL (derivable once instructions is in @context and dereferenceable, so hardcoding it now is a temporary stand-in for prerequisite 1 and should say so); and the workflow file's path (bootstrap's own BPMN is named by bootstrap.jsonld, so hardcoding it separately would be a second answer to a question the graph already answers).

THE MECHANISM ALREADY EXISTS IN THIS REPO AND SHOULD BE REUSED RATHER THAN INVENTED. AGENTS.md on the two duplicated harness directories: '.beans.yml because the beans binary is third-party and will never read our schema, and WORKFLOW_DIR in workflow/store.ts because it is on the hot path ... The duplication is unavoidable; an unchecked one is not.' Both are checked by bun run check:harness-dirs. Apply the same pattern: a hardcoded asset path in bootstrap is fine AND IS CHECKED — a test that the literal resolves to a file that exists and, where it is a URL, that it is among the paths the publish step actually produces. kg-export.test.ts already does the URL half for the whole exported graph (the dead-link walk over PUB), so this is an extension of a working check rather than a new one.

AND THE GATE STILL HELD BACK FROM STEP 1 GETS ITS ANSWER FROM THIS. The remaining piece of step 1 is a gate refusing a NEWLY hardcoded workflow path. The open question was what it does about legitimate literals; an allow-list is what this repo warns against. This settles it: the gate refuses a hardcode that a declaration could have answered, and a literal that is a base case declares itself as one and is checked to resolve. Same rule, one sentence, and it is the owner's 'ok but not preferred' made mechanical instead of remembered.

_2026-09-19T06:54:32Z_ — NO MCP TOOL CALL IN BOOTSTRAP — owner 2026-09-19: 'there should be no mcp tool call in bootstrap. thats cat-harness level stuff'. This is the sharpest constraint on bootstrap so far and it RETIRES THE ONE WORRY I HAD FLAGGED rather than adding a new one. Two notes ago I wrote that step 3 of the README 'has two readers and only one of them has MCP', and argued for naming the process and its first step rather than a tool call. That is now not a preference, it is the rule: EVERY ONE OF THE THREE STEPS MUST BE PERFORMABLE WITH NO MCP AT ALL. Read a .md, load a .jsonld, read a .bpmn and perform its first step. No skill_fetch, no workflow_start, no work_plan_prime.

IT ALSO CORRECTS SOMETHING ALREADY WRITTEN IN THIS BEAN, AND THE CORRECTION IS THE INTERESTING PART. The seven-skill sketch earlier here listed bootstrap's tools as beans-manual (scripts/beans-fallback.ts), beans-cli and folio_init. ALL THREE ARE NOW OUT. folio_init is the one that matters, because it was the entire 'initialize' branch of the gateway — AGENTS.md records it as registered among the GENERIC tools precisely so it is reachable before a folio has a content type, and I had treated that as settling the no-instance branch. It does not: it is an MCP tool, so bootstrap cannot call it. SO THE INITIALIZE BRANCH DOES NOT INITIALIZE. It routes — its first step is 'load the graph that carries folio_init', which is cat-harness.

AND THAT MAKES THE GATEWAY SYMMETRIC, WHICH IS A GOOD SIGN RATHER THAN A CONSOLATION. Both branches now end in the same shape: in an instance -> load its dependency tree; not in one -> load cat-harness so you can initialize one. Bootstrap never DOES the work on either side; it decides which graph to load and hands over. That is 'the FIRST STEP (not all steps)' taken literally, and it is what makes 'no MCP' affordable instead of crippling — a step that only routes needs no tools by construction. If a future draft of the BPMN has bootstrap performing anything, the no-MCP rule and the first-step rule are both being broken at once, and that is the falsifier to check the diagram against.

OPEN QUESTION THIS RAISES, NOT DECIDED HERE: whether bootstrap declares a `tools` graph at all. The earlier sketch had { id: bootstrap, path: bootstrap/, graphs: [cat-harness, tools, schemas] }. If bootstrap calls no tool, `tools` may be wrong there — or it may still be right, because a Tool node is a DESCRIPTION of a tool and describing what exists is exactly what the README's 'tool overview' section does. The distinction is between declaring tools bootstrap CAN CALL and declaring tools it can TELL YOU ABOUT, and the graph kind does not currently separate those. Worth resolving before the directory is created rather than after, since it is one line in cat-harness.json and a wrong one is the dh4f defect shape (a consumer scanning for something that is not there).

NOTE FOR THE README'S 'TOOL OVERVIEW' SECTION: it stays, and it is now unambiguously a POINTER — these are the tools cat-harness gives you once you load it, not tools available here. Written the other way it would promise a capability bootstrap has just been forbidden.

_2026-09-19T06:55:21Z_ — USER STORY DECISION: DEFERRED, NOT TAKEN. Owner 2026-09-19: 'do not adopt SGUserStory now. bean for later as part of bean to get datamodel for DAK. we can relate them later.' So the three options I put up here — adopt smart-base's IRI, mint our own under FOLIO_NS, or mint-with-equivalence — are all OFF the table for now, and NOTHING in bootstrap/ should reference a user story term. The full finding has been moved to its home, bean cz17 ('Migrate dak.json in: the DAK type is ours, and its Logical Model is pending upstream'), which is the DAK data-model bean; it is recorded there in full so that bean does not re-derive it. WHY THAT IS THE RIGHT HOME AND THIS ONE IS NOT: the finding arrived here because the README spec asks for one user story, but the QUESTION it raises — whose vocabulary owns the term, and how our Requirement relates to SGRequirements — is a DAK data-model question. Leaving it here would have made bootstrap's directory design wait on a WHO alignment decision it does not depend on. CONSEQUENCE FOR THE README, and it is small: the 'one user story' section is prose in the README, not a typed node, so it needs no schema and no term. It can be written before any of this is settled. If a later draft finds itself wanting a userStory: { capability, benefit } field to write that section, that is the signal the deferral has been reached, not a reason to reopen it early.

_2026-09-19T07:40:52Z_ — THE STUB PATTERN, AND WHICH DIRECTORIES TAKE IT — owner, 2026-09-19: 'excluding bootstrap as special case to be its own directory, tools/ skills/ library/ docs/ should all use the stub pattern. todo, beans do not use stub pattern'. So a declared directory is subdivided BY INSTANCE STUB — tools/<stub>/, skills/<stub>/, library/<stub>/, docs/<stub>/ — and two are deliberately exempt. This answers the survey question directly: NOTHING is laid out that way today. Measured 2026-09-19: tools/ has ZERO subdirectories, schemas/ has two subdirs and 57 loose files at top level, beans/ is split by node kind, and skills/ is the only fully subdivided one — 13 subdirs, 0 loose files — but its axis is the authoring PACKAGE (folio-core, authoring-math, folio-paper-adapter), not the instance. The layer assignment exists only as a COMPUTATION: repo-partition.ts assigns all 454 modules to five repos by rule and triage over import edges, reading no path. So the layering has had no filesystem counterpart at all.

WHY THE EXEMPTIONS ARE PRINCIPLED, not arbitrary, and worth writing down so nobody 'tidies' them later. The four that take the stub are all OVERLAID: when a folio depends on the platform, both contribute tools, skills, library material and docs, and without a per-instance namespace the contributions collide on any shared filename. The stub is what lets resolveSkillDirs' overlay be a real merge rather than a race. beans/ and todos/ are NOT overlaid — a downstream instance does not inherit its dependency's work plan, and would be actively harmed by seeing it: 'claim before you work' means claiming in YOUR store. An instance's beans are its own, so there is no second contributor to disambiguate from and a stub level would be one empty directory deep for no gain.

COST, MEASURED ON ecb616ac5, and it is very unevenly distributed. tools/ 2 tracked files, 0 subdirs — trivial, do it first. skills/ 483 files in 13 subdirs — a subtree move, and CHEAP IN REFERENCES for a reason already measured on this bean: BPMN skill refs are by BARE NAME (47 distinct, zero containing a slash) and gen-skill-docs publishes FLAT by name (docs/reference/skill-instructions/ has no subdirectories), so moving a skill changes no reference and no published URL. library/ 1402 tracked files across four ingested documents. docs/ 405 tracked files, 219 of them .md pages — the 3943 an earlier count reported was my own gitignored bundler tree, not content.

DOCS/ IS THE EXPENSIVE ONE AND SHOULD NOT BE DONE WITH THE OTHERS. 350 files outside docs/ reference the path, and every published page URL moves with it — the docs site, the Pages base, the skill-instruction pages the bootstrap README is supposed to link to, the staging previews, and every link in every bean and issue that names one. skills/ and tools/ change references not at all; docs/ changes essentially all of them. Sequencing them together would make a cheap move and an expensive one indistinguishable in review, and an unrelated failure in the docs half would hold the rest hostage. Suggested order, each independently revertible: tools/ (2 files), skills/ (483, no reference churn), library/ (1402, see below), docs/ LAST and on its own.

AND library/ RAISES A PRIOR QUESTION THE MOVE WOULD BURY. It holds 1402 tracked files — 9789241548960-eng, who-pub-tps-931, wpr-rdo-2020-003-eng, milnorlink — which is INGESTED L1 CONTENT sitting in the platform repository. AGENTS.md's own banner: 'folio-assistant is the platform, not the content. ... If you are about to write subject matter here, you are either in the wrong repo or writing something that belongs in the folio as data.' Namespacing it under a stub would make it tidier and would NOT make it belong here. Worth deciding whether it moves out before deciding where it sits, because doing the stub move first means doing the work twice.

THE STUB'S VALUE IS NOW A LAYOUT DECISION, NOT A COSMETIC ONE. cat-harness.json declares stub 'folio-assistant' explicitly (it is not derived — artefactStub reads the field). That string has until now named published artefacts: folio-assistant.jsonld, folio-assistant.schema.json, the viewer directory. Under this design it also becomes a DIRECTORY NAME REPEATED IN FOUR TREES. The open question recorded earlier on this bean — folio-assistant or cat-harness — therefore stops being one line in a JSON file and becomes the name a reader meets at every level of the tree. It should be settled BEFORE the first move, not after, because changing it afterwards is the same four moves again.

BOOTSTRAP IS EXEMPT AND THAT IS CONSISTENT with everything else decided for it: it is its own top-level directory, not a stub subdivision of anything, because it is the BASE that other instances are loaded THROUGH rather than one contributor among several. A bootstrap/<stub>/ would be asking which instance contributed the thing you use before you have loaded any instance.

_2026-09-19T09:07:22Z_ — THE SPLIT, REFINED BY THE OWNER, 2026-09-19: 'for separation what is in f-a-core vs f-a. f-a-core has high level processes only, no tooling. f-a has all tooling KG. use tools/<stub> pattern.' So the line between folio-assist-core and folio-assistant is PROCESS versus TOOLING, not content versus platform as an earlier reading of this bean assumed. f-a-core carries the high-level processes — the BPMN layer, what happens in what order and who is accountable — and NO tooling. f-a carries all the tooling and its knowledge graph.

THAT IS A CLEANER CUT THAN THE ONE repo-partition.ts CURRENTLY COMPUTES, and the two should be reconciled before any files move. The script assigns 454 modules across five targets by import-edge rules and hand triage; its folio-assist-core bucket (160 modules) was populated on a content-versus-platform reading, so a module that is tooling-with-content-knowledge sits there today. Under the owner's cut it would go to f-a. Nobody should move directories until the partition's rules encode the process/tooling line, because the partition is the only measured statement of where anything belongs and moving against it would make its 4 remaining wrong-direction edges meaningless.

tools/<stub>/ IS CONFIRMED as the layout for the tooling side, consistent with the stub-pattern decision already recorded here (tools/ skills/ library/ docs/ take the stub; beans/ and todos/ do not; bootstrap/ is its own directory).

A TEST OF THE CUT, since it should be falsifiable rather than a slogan: a BPMN diagram under processes/ is f-a-core. The BPMN LOADER, the workflow engine, workflow_start and the DMN evaluator are f-a. If a module is needed to RUN a process it is tooling; if it DESCRIBES one it is core. The awkward cases to check that against are scripts/render-bpmn.ts (renders a diagram — tooling), processes/*.bpmn (the diagrams — core), and schemas/kg-qa.ts (audits the join between them — tooling, because it is a checker).

_2026-09-19T09:07:23Z_ — STOP — harness.config.json MUST NOT BE RENAMED TO folio.config.json, and this note exists so nobody proposes it a third time. I recommended exactly that on 2026-09-19 and the owner approved a HARD rename, no fallback. Both of us were working from my misreading, and I caught it before touching any of the 332 files.

THE EVIDENCE. Commit 109beee02, 2026-09-18 — the DAY BEFORE — renamed folio.config.json TO harness.config.json, module and symbols together, with its message stating: 'The file configures the HARNESS — adapter selection, the skills directory, the viewer, simulators, translation, and the two work-plan stores. None of that is the folio's content. The old name described the wrong thing and invited people to look in it for settings that were never there.' Commit 211ab2cf3 then DROPPED the folio.config.json fallback. The reasoning also survives in schemas/harness-config.ts above HARNESS_CONFIG, where its first line reads 'The harness config file, and the name it used to have.'

MY ERROR, named precisely so the same mistake is recognisable. I read the _comment FIELD inside this repo's own harness.config.json, which describes content/docs as a document-profile corpus, and concluded from it that the FILE is a content config. That comment is about one field's use in one instance; it says nothing about what the file is for. The schema's doc comment said the opposite and I did not read far enough before recommending.

SO THE PREMISE OF THE RECOMMENDATION WAS FALSE. I told the owner the two root files are structure-versus-content and that the shared 'harness' stem plus a '.config' suffix invites a permanent misreading. They are both HARNESS concerns: harness.json says WHAT DIRECTORIES EXIST, harness.config.json says HOW THE HARNESS BEHAVES. On that reading the shared stem is correct and the pairing is informative rather than misleading, which removes the entire basis for the change.

WHAT WOULD ACTUALLY BE WORTH DOING, if the pairing still reads badly to a fresh eye: the constant HARNESS_CONFIG already exists in schemas/harness-config.ts with resolveHarnessConfigPath beside it, and 110 of its occurrences across 39 .ts files are BARE LITERALS that ignore it. Routing those through the constant makes any future rename one line, which is what DECLARATION_FILENAME bought for harness.json — that rename cost 39 files and one constant edit, against this one's 332. That is a real improvement available at any time and it does not require deciding the name.

_2026-09-19T10:15:04Z_ — BOOTSTRAP INSTANTIATION INTO A REPO THAT IS NOT YET AN INSTANCE — owner, 2026-09-19, three messages that together describe the whole no-instance branch of the gateway, and the third one changes the shape of the other two.

(1) 'when doing a boot strap instantation into a repo that is not an instnace, the bootrap/ skills should have the cat-harness library replicated under bootstrap/cat-harness/ etc. as local cache and dirs gets declared in bootstrap'.

(2) 'the repo_name instance declaation needs to be made in repo once the user's intent on harnes type is known (e.g. folio, smart-guidewline DAK, smart guideline IG or whatever)'.

(3) THE WORKED EXAMPLE, and it is the one to design against: 'typical command woudl be "make this repo into a litlfred/f-a-sci instnace" which would turn it into a folio with the f-a-sci knowldfge grpah loaded and using milnor as voice'.

WHAT (3) CORRECTS. Reading (2) alone, I had the intent skill producing a HARNESS TYPE — folio, DAK, IG — as a value from a closed set. That is wrong, or at least far weaker than what (3) describes. THE USER NAMES AN UPSTREAM INSTANCE, and everything else is READ from that instance's declaration: `litlfred/f-a-sci` yields the harness type (folio), the knowledge graph to load (f-a-sci's), and the voice (milnor). Nobody is asked three questions; one reference answers all of them.

So the intent skill's output is AN INSTANCE REFERENCE, not an enum. That is better in the way this repository keeps rediscovering: a declaration is read rather than a property re-stated. An enum would be a second answer to 'what kind of thing is this repo', free to disagree with the upstream declaration it came from, and a fourth place the harness type is written down.

AND IT MAKES AN EARLIER DECISION ON THIS BEAN PAY OFF RATHER THAN MERELY BEING TIDY. The owner sent milnorlink and voices/milnor.json to folio-asst-sci, on the grounds that a library document, its voice and the skill derived from it are one bundle. (3) is what that buys: 'using milnor as voice' is not a fourth thing the command configures — it is a consequence of naming f-a-sci, because the voice LIVES there. Had milnor stayed in the platform, the command would have had to name it separately and the bundle argument would have been decoration.

THE ORDER IS THEREFORE: instance reference -> read ITS declaration -> write this repo's declaration from what that says -> cache what the declaration needs. Not: ask the type, write a declaration, fetch things. The difference matters because writing a declaration before reading the upstream one leaves a window in which the repo declares something it is not, and every consumer that reads a declaration would believe it.

THE CACHE IS bootstrap/cat-harness/, WHICH IS THE STUB PATTERN AGAIN — one subdirectory per contributing instance, the same shape as tools/<stub>/ (shipped 2026-09-19) and the skills//library//docs/ plan. So bootstrap invents no mechanism for vendoring; it uses the one in place, and the cached copy is DECLARED in bootstrap's own harness.json so ordinary resolution finds it with no special case. A cache needing its own lookup path would be a second answer to 'where do skills live'; a cache that is just another declared directory is not. Under (3) the cached directory is named for the instance it came from — bootstrap/f-a-sci/ alongside bootstrap/cat-harness/ — which is what the stub pattern is FOR and what makes two upstreams composable rather than colliding.

TWO THINGS STILL UNANSWERED, recorded so they are not rediscovered.

STALENESS, the question every cache owes. This repository's doctrine is that a copy with no staleness check drifts and is believed anyway — render:bpmn:check exists for that, translate-bpmn's missing equivalent is bean 0hd6, and check:harness-dirs gates the two duplicates AGENTS.md says cannot be removed. 'It was copied at init' is not an answer. The cheapest honest version records the source ref and compares against it, reporting COULD NOT DETERMINE when the upstream is unreachable rather than silently passing.

WHAT SUBSET IS REPLICATED. f-a-sci's knowledge graph is not all of cat-harness, and a bootstrap that copies everything stops being lean in the way the owner asked for ('bootstrap is super lean, referencing only the minimum of schema'). Under (3) the named instance's own declaration is the obvious selector — it already says which directories it has — which would mean the subset needs no separate rule. Worth checking that claim against a real f-a-sci declaration before relying on it.

## Note — 2026-09-19, the gate

THE GATE IS SHIPPED — `check:declared-paths`, 2026-09-19, and it is the piece this bean named as next after `workflowDirs()`.

WHAT IT DOES. Reads `harness.json`, walks this instance's `.ts`, and classifies every string literal naming a declared directory into three buckets. 49 name a FILE that exists (authored prose naming one artefact, which no declaration would answer) and are CHECKED TO RESOLVE — that closes the `blv9` class over this corpus as a property of the corpus rather than as one run's result. 27 carry `declared-path-literal: <reason>` and are the base cases. 125 are unaccounted for.

WHY A RATCHET AND NOT A WALL. 125 is the finding: the debt is an order of magnitude past the nine sites the rewire knew about. It is also why a blocking gate cannot land — some sites need real design (`LOCAL_PACKAGES` in skill-fetch.ts is a package->directory map, not a lookup), and a gate that fails 125 times on the day it arrives is a gate somebody switches off. Baseline committed PER FILE rather than per line, because a count churns far less under an unrelated edit than a line number does; a file may only lose literals.

THE RULE, from the owner's hardcoding concession ("if need to hardcode json/jsonld assets, thats ok. not preferrred"): permitted and discouraged means DECLARED AND CHECKED, not forbidden and not free. The reason is required and marked sites are printed on every run, so silencing costs more than satisfying — the same shape as `<folio:no-skill reason>`.

TWO REAL DEFECTS FOUND ON THE FIRST RUN, which is the argument for the gate. (1) `src/impact/stakeholder-map.ts` listed four skill directories where `skillMdDirs()` finds six, so `authoring-math` (3 skills) and `authoring-who-smart-guidelines` (9) were invisible to the impact analysis: changing one reported no roles, no lanes, nobody accountable — indistinguishable from a change affecting nobody, the one wrong answer that tool must not give. (2) Reading the declaration then admitted `.claude/skills/local/`, and `todo-manager.md` exists THREE times, so the `name -> path` index silently dropped two. Keyed by PATH now.

AND ONE I HAD JUST INTRODUCED. `ab62c9dfe` rewired `check-workflow-refs`'s NOT-INDEXED scan to `workflowFiles()`, which returns ABSOLUTE paths, and left the prose comparison alone — all 32 diagrams read as unindexed. Fixed by matching basename again, but only where the basename is UNAMBIGUOUS: two `review.bpmn` under a topical layout are indexed by neither a single mention.

WHAT THE 125 ARE, roughly: 16 generate-docs, 11 repo-partition (now marked), 9 kg-audit, 7 each check-tools / generate-registry / skill-fetch, 6 known-skills, 5 each validate-skills / harness-schema-export / mcp-server, 4 each translate-bpmn / translation / document adapter. Draining them is not one task: skill-fetch's is a design question, the adapters' `uploads` is a different declaration, and generate-docs is mostly output paths.

## Note — 2026-09-19, the library/ extraction inventory

MEASURED, not planned: the move cannot be executed from this session. `who-style-guide` and `folio-asst-sci` DO NOT EXIST, and this session's repo scope is `litlfred/folio-assistant` and `litlfred/folio-test` only. So this records what would move and what would break, and the repo question goes to the owner.

WHAT IS THERE. Four slugs under `library/`, 1401 files, ~5.9M:

| slug | files | ocr/ | what |
|---|---|---|---|
| `9789241548960-eng` | 752 | no | WHO Handbook for Guideline Development, 2nd ed, 179pp |
| `who-pub-tps-931` | 486 | YES (121) | WHO Editorial Style Manual, 121pp — the ONLY scanned source |
| `wpr-rdo-2020-003-eng` | 101 | no | WHO W-Pacific Publication Style Guide, 33pp |
| `milnorlink` | 62 | no | Milnor, "Link Groups", Annals of Math 59(2) 1954 |

Four voices, 1:1 onto the slugs, no voice citing two: `who-editorial` -> `who-pub-tps-931`, `who-guideline-development` -> `9789241548960-eng`, `who-publication-design` -> `wpr-rdo-2020-003-eng`, `milnor` -> `milnorlink`. So the owner's split is clean on the voice axis: three WHO bundles leave together, one milnor bundle leaves alone.

WHAT BREAKS, and it is remarkably little. EXACTLY ONE TEST: `scripts/tests/qa-checkers-voice.test.ts:527` reads `library/milnorlink/sections` from the real corpus and asserts `hits === 1`. Deleting the slug makes `readdirSync` throw ENOENT — it ERRORS rather than merely failing, which is worse to debug. NO test touches any WHO slug: every other library-reading test builds fixtures under `mkdtempSync` with invented slugs (`who-anc-2016`, `doc-1`, `src-1`, `0110001v3`). `scripts/tests/pdf-doc-id.test.py` asserts the STRING `who-pub-tps-931` and the path `library/who-pub-tps-931/ocr` but never stats them.

A LIVE DEFECT FOUND ALONG THE WAY, and it is worth a bean of its own. `scripts/check-voices.ts` validates the whole voice graph against disk — 36 of 37 voice rules cite a `library/<slug>/sections/<id>.md`, and every voice source requires a `structure.json`. It is wired as `package.json` `check:voices`. It appears in NO CI workflow: `grep -rn 'check:voices\|check-voices' .github/` returns ZERO, verified independently of the survey that reported it. So the one gate that would catch this extraction breaking the voice graph is a gate nobody runs. That is the `xom7` shape (a red workflow looks like a green one from in here) with the workflow missing entirely rather than failing.

THE INGESTION-FIXTURE ARGUMENT STILL HOLDS AND SHARPENS. The earlier note on this bean recorded the owner's "current OCR etc pipeline is jsut one set of tools. another is coming", and concluded something must stay as the corpus both pipelines are measured against. The inventory names which: `who-pub-tps-931` is the ONLY slug carrying OCR output at all (121 pages). Lose it and the second pipeline's OCR half has nothing to be compared on. So if the three WHO documents leave as a bundle, the platform needs either a retained copy or a synthetic OCR fixture BEFORE the move, not after.

BLAST RADIUS OF THE NAMES, 25 non-test files. The heaviest are `content/pipeline/qa-criteria-registry.ts` (17 hits: four `voice-overlay-*` criteria plus `expo-milnor-clarity` and `milnor-brevity`) and three folio-core skills that link `library/milnorlink/` and `voices/milnor.json` by relative path — `milnor-exposition-standard.md`, `exposition-swarm-drain.md`, `one-voice-style-guide.md` — each with a generated mirror under `docs/`. `.claude/` has ZERO references. No BPMN element id encodes a slug; the two mentions are prose in `<bpmn:documentation>`.

WHAT I WOULD DO, for the owner's call: create the two repos, move the bundles, keep `who-pub-tps-931` (or a reduced OCR fixture from it) in the platform as the ingestion corpus, put `check:voices` into CI FIRST so the move is gated rather than hoped, and convert the one live test to a fixture so the platform's test suite stops depending on a document that is leaving.


_2026-09-19_ — PROPOSAL UPDATED with the owner's four additions (§7 new, §0 and §5 extended, §9 status). Measured first, and two things were NOT what I had assumed. (1) bootstrap/ ALREADY EXISTS — one file, README.md, the spec, marked "the graph is not built". I nearly designed a directory that was already there. (2) The proposal ALREADY says most of "bootstrap is an instance": §1 "it declares itself, and cat-harness is an instance loaded through it rather than its parent", §6 "declared in bootstrap's own harness.json". So the owner's "bootstrap should be an instance and contain its own definitions" SHARPENS existing design rather than introducing it — saying *instance* rather than *standalone graph* names a mechanism this repo already has (compose, inherit, override BY ID). I left §1's wording as written and added a forward pointer, rather than rewriting it: everything it says stays true, and a rewrite would hide that the design reached for a mechanism before naming it. WHAT WAS GENUINELY NEW, none of it anywhere in the 242 lines: AGENTS.md as a declared asset; copy as the default behaviour; the one-file-two-phases constraint; and the fifth flow step (re-enter through the now-active harness). THE FINDING THAT MATTERS: the root AGENTS.md WE SHIP TODAY WOULD FAIL as bootstrap/AGENTS.md. Its cold-start line is scripts/install-beans.sh && beans prime, and its "Getting skills" box says use skill_list/skill_fetch. A repo mid-bootstrap has no scripts/, no skills/, no beans, no MCP — and §4 forbids a tool call anywhere in bootstrap for exactly that reason. Every one of those instructions is post-harness. So phase 1 must be self-sufficient and phase 2 points into the KG, and they are reconciled by the fifth step rather than by a second file: the file is ADDED TO, never replaced. TENSION RAISED RATHER THAN PAPERED OVER: "default behaviour is to copy" runs straight into the proposal's own §8 — "'it was copied at init' is not an answer" — and that is not hypothetical for this file, since v8gh measured seven dead links in the root AGENTS.md, five broken by one directory move, including the one its own banner calls the place to start. Declaring it as an asset is what makes the check FOLLOW rather than be special-cased; runReadmeAudit already takes an arbitrary `file`, so no new checker is needed. Added a fourth falsifier to §0 accordingly: if phase 1 turns out to need DELETING rather than extending, this is a template not an asset and the staleness argument collapses. AND THE USEFUL SCHEDULING FACT, now written into §9: §7 does NOT wait on the resolveSkillDirs blocker. Declaring the bootstrap instance, declaring AGENTS.md as its asset, and authoring a phase-1-valid bootstrap/AGENTS.md are all statements about bootstrap's OWN declaration — none resolves a dependency's skills. That makes §7 the first buildable slice, worth saying explicitly because a proposal whose every part waits on one blocker tends to wait entirely. Verified: readme:audit, check:declared-paths, check:agents-xref, kg:audit:check all rc=0; every relative link in the proposal resolves (checked directly, since "nobody checks the links in a file like this" is the v8gh defect); full suite 2552 pass 0 fail. Related: issue #223 "separation of concerns", whose Phase II is "separate repos, turn them into folio-assistant instances" — this is that move applied to the smallest unit. NOT built yet: next is wiring resolveSkillDirs (owner's order: proposal, then unblock, then slice).


_2026-09-19_ — resolveSkillDirs: the HARDCODED PATH is fixed; the NO-CALLER half is not, and the reason is worth recording rather than forcing. What I found on opening it: `resolveSkillDirs` did `join(dep.rootPath, "skills")` — the literal AGENTS.md names explicitly ("hardcoding a path is how a skill goes missing the moment the layout moves"), in the one function whose whole job is cross-instance resolution. And `kgDirectories` in scripts/known-skills.ts called `resolveDirectories([{name:"(local)", root, own:true}])` — declaration-driven but a CHAIN OF ONE, which is exactly what AGENTS.md means by "skill discovery is root-only in practice". THE LOAD-BEARING FINDING, and it explains why this function sat unused for so long: resolveDirectories CANNOT be the primitive for an overlay. It overrides BY ID (`byId.set`), so a dependency and a root both declaring `cat-harness` collapse to the root's entry alone and the dependency vanishes. That is correct for its own question — "which directory does id X mean for this instance" — and precisely wrong for "load every instance's skills in order". Two different questions; one primitive was being asked the wrong one. So the old code reached for a literal to get per-instance paths, because the declaration-driven route it had would have eaten the dependency. Added `ownDirectories` (one instance, no inheritance, no override) + `isKgOnlyDirectory` + `KG_GRAPH_KIND` to schemas/cat-harness.ts, and rewrote resolveSkillDirs on top of them: declaration-driven, overlay-preserving, de-duplicated for diamond dependencies, and still honouring `provides` (the only way to depend on an instance for content without inheriting its skills). A REGRESSION I MADE AND THE EXISTING SUITE CAUGHT: my first version returned [] for an instance with no harness.json, silently dropping every UNMIGRATED dependency's skills. harness-config.test.ts's overlay test went red — its fixture declares nothing. AGENTS.md is explicit that "absent declaration is fine; an unmigrated instance falls back to today's conventions", and DEFAULT_DIRECTORIES already carries {id:"cat-harness", path:"skills/"}. Reading a declaration is the improvement; REQUIRING one would have been a breaking change wearing its clothes. Fixed, and pinned with its own test naming it as a regression so it cannot return quietly. WHAT I DELIBERATELY DID NOT DO: give it a consumer. The two obvious call sites each break in a specific way. (1) kgDirectories composes IDS — its `path` becomes a repo-relative skill id via skillMdDirs, so feeding a dependency's directory in would mint ids that look repo-relative but resolve into another checkout, wrong in a way that reads as correct. (2) kgRoots(root)[0] is taken as "the root's graph" by src/tools/workflow.ts and src/workflow/gate.ts, and overlay order is deepest-dependency-FIRST, so making it overlay-aware hands those callers a DEPENDENCY's role graph. The real consumer is LOCAL_PACKAGES in src/tools/skill-fetch.ts, a hardcoded map and a larger change. Wiring either of the two to turn the status table green would be the measurement-shaped mistake this repo keeps naming: a green cell over a consumer resolving the wrong thing. The table now says ⚠ with both hazards written into the function's docs. TESTS NEED A SYNTHETIC FIXTURE AND THAT IS THE POINT: this repo declares no dependencies, so every assertion would pass vacuously against the broken version on the real tree. 8 tests on a two-instance fixture, including one asserting resolveDirectories WOULD collapse them — pinning the reason, not just the behaviour, so a later "simplification" is told what it broke. FALSIFIED: restored the old hardcoded implementation, "a knowledge graph NOT at skills/ is still found" went red, 6 pass 1 fail; restored, 8 pass. Only one test fails against the old version, honestly — its defect was the literal, not the ordering. Verified: tsc, eslint, kg:audit:check, check:schema-nodes all clean; full suite 2560 pass 0 fail.


_2026-09-19_ — STEP 1 BUILT: bootstrap is a declared instance and AGENTS.md is a declared asset. §0's FOURTH FALSIFIER DID NOT FIRE, and that was the thing to watch — I added it an hour earlier precisely so this could fail honestly. It says: if a phase-1 AGENTS.md needs instructions that only make sense post-harness, then the copy is a TEMPLATE rather than an asset and the staleness argument collapses. Writing it, phase 1 turned out to be writable as an ADDITIVE file, and what makes it work is that its content is almost entirely about what is NOT decided yet: three files you can rely on, one flow to read, one reference to obtain, and an explicit "if you are reaching for a rule about content types, voices, review gates or QA — that rule is not bootstrap's; install the harness and ask it". A phase-1 file that tried to be USEFUL about harness concerns would have needed deleting later; one that is disciplined about its own ignorance only needs adding to. WHAT SHIPPED: KgAssetSchema + AssetSourceSchema in schemas/kg-node.ts (NOT a widened KgImageSchema — an image carries layout/width/height/textRegion because a renderer picks between crops, and an AGENTS.md has no crops, so widening would give every asset four fields that are meaningless for it and optional for images, leaving nothing able to tell a missing width from an inapplicable one; the VOCABULARY is shared deliberately — id, src, role, labels — because two spellings of `role` is the drift this repo keeps paying for). `assets[]` on CatHarnessDeclarationSchema AND on the hand-written CatHarnessDeclaration interface, which are two places that can drift — tsc caught the second, which is how I learned the type is not inferred from the schema. `declaredAssets(root)` reports existence rather than FILTERING on it, unlike the directory defaults, and the distinction is real: a declared asset that is missing is a FINDING because somebody asserted the file is ours, whereas an absent conventional directory is just a convention this instance did not take up. bootstrap/harness.json declares NO directories deliberately — bootstrap holds files, not graphs of its own, and a declared-but-absent directory is the dh4f defect. Root harness.json now declares AGENTS.md with `source` naming bootstrap/AGENTS.md, so "is this still what it was copied from" is a question that can be ASKED. THE v8gh PAYOFF, and it is the part worth keeping: scripts/check-declared-assets.ts is NOT "a link checker for AGENTS.md" — that would special-case the file this design stops special-casing. The gap was never "nobody wrote an AGENTS.md checker"; it was that the file was UNDECLARED, so no checker had any reason to look at it. It checks whatever an instance declares, so a third markdown asset tomorrow is covered with no change. Three states, middle one load-bearing: missing / dead-link / NOT CHECKED (external or non-markdown), the last never counted clean. FALSIFIED BOTH WAYS before trusting: appended `../docs/guides/agent-onboarding.md` to bootstrap/AGENTS.md — the REAL path from v8gh, the one AGENTS.md's own banner calls the place to start — rc=1 dead-link; moved README.md away — rc=1 missing; restored — rc=0. Gated in CI from the first commit rather than after a backlog, because declarations and gate land together so it starts at zero findings rather than measuring debt (unlike agents-xref, which had to wait for 20→0). Verified: 9 new tests, full suite 2569 pass 0 fail, tsc/eslint clean, and check:declared-assets, check:declared-paths, check:harness-dirs, check:schema-nodes, kg:audit:check, readme:audit, check:agents-xref, check:workflows all rc=0. STILL NOT BUILT, so the bootstrap README now says "partly built" rather than "not built": bootstrap.jsonld, the two skills, the BPMN. And init-folio does not yet COPY the file — declaring came first, deliberately.


_2026-09-19_ — THE OVERLAY NOW HAS A CONSUMER. `LOCAL_PACKAGES` in src/tools/skill-fetch.ts is built from `resolveSkillDirs`, so a DEPENDENCY's skill packages are served — the Phase 0.1 gap AGENTS.md describes. MEASURED BEFORE TOUCHING ANYTHING, and the first measurement said DO NOT do the obvious thing: a plain scan of the declared kg directory GAINS seven non-package directories (roles, workflows, permissions, requirements, framework, remote-packages, memory — the last already on record for making kg-audit write 25 bogus sidecars) and LOSES folio-assistant, which points at src/skills outside the declared tree. So the hardcoded map was not only laziness: it encoded two facts discovery did not have. SECOND MEASUREMENT found the fix already existed: filtering candidate directories with `isSkillMd` — declaration over location, a .md carrying `$schema:` is stating it is something else — makes discovery reproduce the hand-written table EXACTLY, no gain and no loss. That equality is the evidence this is a refactor rather than a behaviour change, and it is why the change was safe to make at all. src/skills remains a named exception; declaring it was probed and trips the declared-paths ratchet in three files, so it is now bean `osbo` rather than scope creep here. THREE TESTS WENT RED AND NONE WAS A BEHAVIOUR BUG — all three were SOURCE-TEXT PROXIES tripping on my documentation comment, which names remote-packages as one of the seven directories a naive scan would wrongly admit. The behaviour they guard was untouched: the derivation correctly excludes skills/remote-packages/, which holds two .json manifests and no body. The repo had ALREADY learned this lesson in a sibling assertion — manifest-remote-resolution.test.ts:144 says "Grepping for shallow-clone was the first attempt and is not evidence: it cannot tell an implementation from a comment, and it flagged the comment this very change added" — but the surrounding tests kept the naive form. Narrowing to quoted strings is NOT enough either, and that is the subtle part: a markdown code span in a comment is delimited by backticks, and backticks quote strings in TypeScript. So the comment has to go first. Added `codeWithoutComments` to scripts/repo-files.ts, which SKIPS string literals rather than stripping them — a naive //-to-end-of-line rule cuts "https://example.com" in half and invents a finding — with 5 tests including the escaped-quote and comment-marker-inside-a-string cases. A REAL FINDING FELL OUT OF IT: with comments stripped, scripts/gen-skill-docs.ts drops off the recorded list of five "readers of skills/remote-packages/". It never read the directory — its only mention is the comment at ~line 209, "remote-packages/ and memory/ are other node kinds". The list was counting prose. It is four now, and the list says what it claims to say. Verified: 2581 pass 0 fail, tsc and eslint clean, and kg:audit:check, check:declared-paths, check:declared-assets, check:agents-xref, check:schema-nodes, check:harness-dirs, check:tools, readme:audit all rc=0. The harness-config status table's Skills row is ✅ for the first time, and the two call sites that must STAY root-only (kgDirectories composes ids; kgRoots[0] is read as "the root's") are documented on the function rather than left to be rediscovered.


_2026-09-19_ — THE TWO SKILLS EXIST, and the measurement changed the layout before I wrote a line of the graph. THE FALSIFICATION CONDITION I SET DID NOT FIRE but a different finding did. What I checked first, per the brief: are .jsonld files hand-written here? NO — kg-export.ts generates `_kg/<stub>.jsonld` from the graph, and CI has a "`.jsonld` siblings in sync with `.ts` manifests" gate. So bootstrap.jsonld should FALL OUT of bootstrap's nodes rather than be authored, which reframes the work: the deliverable is the two skill BODIES, and the graph is a projection of them. THE BLOCKER FOR THAT HALF: `const ROOT = join(dirname(fileURLToPath(import.meta.url)), "..")` — kg-export is HARDCODED to the repo root. It takes --out and --base-url but no --root, so it cannot export any instance but this one. That is the bgle shape again (a hardcoded path in the one tool whose job is cross-instance), and it is a genuine fork I put to the owner rather than deciding alone: add --root, or hand-write the file under §4's "permitted but not preferred". THE FINDING THAT MOVED THE FILES: isSkillMd returns TRUE for bootstrap/README.md and bootstrap/AGENTS.md. Its rule is declaration-over-location — a .md that does NOT declare `$schema:` is a skill — and neither of those declares one. So a cat-harness graph rooted at `bootstrap/` would count this instance's two DECLARED ASSETS as skill nodes, which is the instance contradicting its own declaration. Two mechanisms I touched the same day, colliding: assets[] says what those files ARE, and the skill scan would say otherwise. Resolved by LAYOUT rather than code — skills live in `bootstrap/skills/`, assets stay at the instance root, and the two cannot meet. The alternative was teaching isSkillMd about declared assets, which is more principled and a bigger change; recorded here so it is a choice rather than an oversight. DECLARED EXPLICITLY, not by default: DEFAULT_DIRECTORIES carries cat-harness→skills/, but ownDirectories honours a declaration AS WRITTEN and only falls back when there is none — bootstrap has a declaration, so an empty `directories` meant an empty graph. resolveSkillDirs("bootstrap") now returns bootstrap/skills, through the same overlay machinery fixed earlier today. THE SKILLS THEMSELVES honour §4's no-MCP rule: kg-navigation is three checkable facts plus a four-row table separating "not an instance yet" from "declaration will not parse" from "declared directory is absent" — the last being the loudest, because a consumer scanning a missing directory reports a clean run. determine-intent produces ONE instance reference and refuses to guess a default, because a wrong instance does not fail, it succeeds at being the wrong thing. Verified: full suite 2783 pass 0 fail; tsc clean; kg:audit:check, check:declared-paths, check:declared-assets, check:bean-parents, check:harness-dirs, check:agents-xref all rc=0.


_2026-09-19_ — STOPPED AT THE GRAPH, and the stop is the finding. I estimated the bootstrap.jsonld fork TWICE and was wrong both times, so I have stopped estimating it and opened bean `gn4l` with the measurement instead. FIRST WRONG ESTIMATE: I called kg-export's hardcoded ROOT "the bgle shape" and recommended adding --root. Measured: 27 references to ROOT across ~20 functions that take no root parameter, AND those references include .claude/skills/, skills/, package.json, SKILL_IO_DIR, auditSchemaNodes(ROOT) and git — none of which a minimal instance has. So it is not root-hardcoded, it is WRITTEN FOR THIS INSTANCE'S SHAPE, and --root would make it export bootstrap as if bootstrap were folio-assistant. SECOND WRONG ESTIMATE: I fell back to "hand-write it, §4 permits it, the file is small". Measured: bootstrap's vocabulary carries 8 CLASSES (Actor, Role, Skill, Process, ProcessNode, SequenceFlow, Directory, GraphKind) and ZERO PROPERTIES. A graph needs properties to relate a process to its steps and a skill to its body, so hand-writing requires minting terms into schemas/vocabulary.ts, gated by ns:check and by kg-export's own undeclared-terms pass. Both paths are real work; neither is the small one I described. WHAT IS GOOD NEWS: the vocabulary machinery already exists and bootstrap already OWNS its namespace — BOOTSTRAP_NS with the `bs:` prefix, one namespace per layer, and termIri() as "the single call every minting site makes" which replaced 93 hand-written template literals. The owner asked for exactly this on 2026-09-19 ("bs:Actor for bootstrap? cat: for catharness?"). And the 8 classes bootstrap owns include Process, ProcessNode and SequenceFlow — so the §3 BPMN decision tree is expressible as graph nodes rather than needing a separate .bpmn mechanism. That is worth knowing before anyone builds it. WHAT SHIPPED INSTEAD, and it stands on its own: the two skills, in bootstrap/skills/ as a declared cat-harness graph. PR #422, green.


_2026-09-19_ — bootstrap.jsonld EXISTS, generated and committed. The falsification condition from the brief did NOT fire: I expected the document wrapper to be as instance-bound as the collectors were, which would have made this a second gn4l pass. Measured instead — buildContext's only "ROOT" match is a COMMENT ("the DOCUMENT ROOT's own fields"), so it is fully generic, and readSourceProvenance's `cwd: ROOT` for git is CORRECT for bootstrap too since bootstrap lives inside this repository. So: wiring, not a seam. THE DESIGN DECISION, and it turned on one measured fact: `_kg/` IS GITIGNORED. The main graph is a build artefact, regenerated every build, and nothing needs it in a fresh clone — which is why nothing gates it. Bootstrap's graph is the opposite case for the reason bootstrap exists at all: its reader has just been pointed at a repository and has nothing installed, so it cannot run `bun install`, let alone kg:export. A graph that only appears after a build is a graph that reader NEVER SEES, and README step 2 says "load bootstrap/bootstrap.jsonld". For that instruction to be true in a fresh clone the file has to be IN the clone. So it is committed. AND COMMITTED FORCES PURITY, which is the part worth keeping: a committed generated file drifts unless something checks, so it needs --check, so it must be a pure function of its inputs. Two fields the main export carries are therefore deliberately ABSENT. `generatedAt` — a timestamp makes every run a diff, so the check would fail on a tree nobody touched and be switched off within a week. `sourceCommitSha` — a committed generated file CANNOT carry its own commit: the best it could name is the commit BEFORE the one containing it, which is wrong by construction and invites a consumer to check out a commit where this file says something else. Its provenance is that it is in the repository; git already answers "which commit is this" exactly. A BUG CAUGHT BEFORE SHIPPING, and it would have been nasty: the "no .bpmn directory" problem string embedded an ABSOLUTE path (/home/user/folio-assistant/bootstrap), so CI — a different checkout path — would have failed the staleness gate on an untouched tree, and the obvious "fix" would have been to weaken or delete the gate. Now repo-relative, and a test asserts no value in the document is an absolute path from the build machine. FALSIFIED BOTH WAYS before trusting: tampered with counts → rc=1 stale; removed the file → rc=1 missing; restored → rc=0. Gated in CI beside check:declared-assets. THE GRAPH TODAY: 2 Skill, 1 Directory, 16 GraphKind, plus `omitted: [registry, packages, schemas, tools]` and one honest problem (no BPMN). The README now says exactly that rather than implying the process node is there — §3's decision tree is still to build, and bootstrap already owns Process/ProcessNode/SequenceFlow in its `bs:` namespace, so it is expressible as graph nodes when somebody does it. Verified: 8 new tests, full suite 2811 pass 0 fail, tsc and eslint clean, and bootstrap:graph:check, check:declared-assets, check:declared-paths, kg:audit:check, ns:check, check:bean-parents, check:workflows, check:agents-xref all rc=0.


_2026-09-19_ — A SIBLING BUILT §3 FIRST, and my branch became something better than what I set out to do. I claimed x3bd and wrote the decision tree; while I did, PR #413 merged with `bootstrap/workflows/bootstrap.bpmn` already in it. My diagram was a DUPLICATE and I dropped it rather than arguing for it — bean-coordination is explicit that a claim ANNOUNCES rather than reserves until the PR exists, and theirs existed first. What survived is the part the sibling's work did not cover, and it turned out to be a live defect in main. THE LEAK WAS REAL AND MEASURED ON MAIN, not predicted: `findBpmnDirs` walked the filesystem — every directory to depth 4, minus a skip list of NAMES — so `bootstrap/workflows/bootstrap.bpmn` was discovered from the REPOSITORY ROOT as well as from bootstrap. I checked out main, ran kg:export, and counted **88 references to `Process_Bootstrap` in `_kg/folio-assistant.jsonld`**. Bootstrap's process was being published as part of folio-assistant's graph. That is the failure I predicted before writing my own diagram, and it was already happening with the sibling's. THE REPAIR THAT SUGGESTS ITSELF IS WRONG: `skip.add("bootstrap")` — a directory name written in code, the defect check:declared-paths exists to refuse, and a line needed again for every instance ever added. Reading the declaration needs none: an instance's diagrams are the ones it DECLARES. TWO PARTS, because one alone would have been worse than the leak: switching findBpmnDirs to workflowDirs(root) stops the leak, but bootstrap's `workflows/` was UNDECLARED, so on its own that would have silently dropped bootstrap's process from bootstrap's own graph — isolation achieved by losing the node. So bootstrap now declares `cat-harness-workflows → workflows/`, a separate id because overrides match on id and `skills/` already holds `cat-harness`. Measured after: leak 88 → **0**, bootstrap's own graph keeps the process (89 references), and the root keeps all 33 of ITS processes. THE SECOND GAP, argued by the checker's own comment: "a dangling <folio:skill ref> in a diagram this checker never opens is a broken reference reported as clean, which is the exact failure this script exists to prevent." check-workflow-refs was root-scoped, so the sibling's bootstrap diagram was unchecked from the day it was written. It now loops over instances, each resolving against ITS OWN skills rather than the union — a bootstrap diagram naming a harness skill is a REAL dangling ref, because bootstrap runs before the harness exists. Falsified on the sibling's actual diagram: pointed A_ReadDeclaration at `todo-manager`, reported DANGLING, exit 1; restored, exit 0. AND I BROKE MY OWN TEST IN THE WAY I HAVE WARNED ABOUT TWICE TODAY: kg-export-instance.test.ts pinned bootstrap's Directory count at exactly 1, which was an incidental fact about bootstrap that day rather than the property. Declaring `workflows/` made it 2. Rewritten to assert the PROPERTY — bootstrap's declaration is its own and strictly smaller than the root's — because a pinned count makes "the isolation holds" and "somebody changed a declaration" indistinguishable. Verified: full suite 2834 pass 0 fail; tsc and eslint clean; bootstrap:graph:check, check:workflow-refs, check:workflows, check:workflow-policy, check:declared-paths, check:declared-assets, kg:audit:check, ns:check, check:bean-parents, check:agents-xref, check:harness-dirs all rc=0.


_2026-09-20_ — THE FIXED CONVENTION: an instance's initialization instructions are ALWAYS at `<stub>/docs/bootstrap/initialization.md`. Owner's ask, and it closes the half of §5 that was open: the single reference answers WHICH instance, and nothing answered WHERE IN IT the setup steps are. bootstrap/README.md step 3 said "read *that* repository's setup file" — which is not a location, so an agent needed per-instance knowledge of every harness it might be pointed at, exactly what one reference was meant to remove. WHAT THE CONVENTION BUYS, in the owner's words: bootstrap can initialise into a SPECIALISED cat-harness node kind. You do not need to know what f-a-sci or smart-base IS, or how they differ — each keeps its own instructions, in its own words, at the same path. A harness nobody has written yet works with no change to bootstrap. COMPOSED, NEVER SPELLED, and the falsification condition in my brief is why: the stub pattern INVERTED on 2026-09-19 (docs/<stub> → <stub>/docs, bean wggr), so every literal spelling of the old layout had to be hunted down. `initializationDoc(decl)` builds it from siteDir(), the one function that knows the site root, so the next relocation moves it for free. Verified it resolves: this instance → folio-assistant/docs/bootstrap/initialization.md; a named target → f-a-sci/docs/bootstrap/initialization.md. CONVENTIONAL *AND* DECLARED, which is not redundant: the convention means bootstrap finds the file without reading a declaration first, and the asset declaration means check:declared-assets verifies it exists and that its links resolve. A convention nothing checks is a convention that is honoured until it quietly is not. This instance now honours the convention it publishes — a convention its own author does not follow is not a convention. MY OWN TEST WAS A WEAK PROXY AND THE FALSIFICATION CAUGHT IT. First version asserted `readme.toContain("/docs/bootstrap/initialization.md")`. I changed the README's stated convention to `<name>/docs/setup.md` and the test still PASSED — because the old path survived in a worked example further down the page. A presence check over prose is satisfied incidentally. Rewritten to parse the FENCED BLOCK, the one place the README actually makes the claim; re-falsified, and it now fails as it should. Same class of error as the source-text greps this session already paid for with codeWithoutComments. Verified: 7 new tests, full suite 3003 pass 0 fail; tsc and eslint clean; check:declared-assets, check:declared-paths, bootstrap:graph:check, check:agents-xref, kg:audit:check, check:bean-parents, check:workflow-refs, readme:audit all rc=0.


_2026-09-20_ — BOOTSTRAP SIMPLIFIED TO ONE PROCESS, per the owner's spec. README is 97 → 66 lines and its shape is now: five terms in a table, then "open workflows/initialize-harness.bpmn and begin at its start event", then the two things the process will send you to. THE SINGLE-BPMN RULE IS LOAD-BEARING, not tidiness: an Initiator that has read the README is at one start event with nowhere else to go, so it cannot begin the wrong process. THREE ROLES DECLARED rather than left implicit in lanes, which is where they lived until today — a lane name is a string the audit cannot bind, so "Bootstrapping agent" and "Person asking (judgement only)" appeared in the graph as roles nobody had defined. Initiator (agent) carries kg-navigation + confirm-harness; Knowledge Graph Data Store (system) is `actedUpon` — a git repo holds and serves and takes no part in deciding, so it carries no skills and naming one would lie about what it does; Requestor (person) is `judgementOnly` — WHICH harness is a judgement about intent and no instruction body produces it. `determine-intent` → `confirm-harness`, reshaped to the owner's contract: IN 0+ harnesses and 0+ locations from the Initiator, OUT **0 or 1** harness from the Requestor. At most one, and the Initiator may not break a tie. THE DEFECT MY OWN CHANGE EXPOSED, and it is a real one: declaring roles with `lanes` produced THREE DANGLING `bindsLane` LINKS in folio-assistant's graph. Cause: the ROOT declares `bootstrap/skills/` as one of its own kg directories (a sibling's deliberate decision, with a long rationale) but does NOT declare `bootstrap/workflows/` — correctly, since #432 removed exactly that leak. So the root reads bootstrap's ROLES but not the LANES they bind to. The root carries HALF of bootstrap, and the two halves are not independent: the same asymmetry is why bootstrap's kg-navigation collides with folio-core's in the root's published docs (bean v3se). Bootstrap's own graph binds correctly either way — 0 dangling — because it carries both halves. WORKED AROUND, NOT FIXED, and said so: `lanes` omitted with the reason in the file, rather than widening the dangling-link allowance, which would record new debt as progress. Bean `pve3` carries the decision — both halves or neither — and notes that the declaration's rationale was written before bootstrap had a process, a role graph and a committed graph of its own. Bean `lv3j` carries the owner's precondition question: BPMN has no precondition element and this schema adds none, so it sits in documentation prose an engine cannot check; the bean's sharpest point is that "understands what a role is" is NOT machine-checkable and a schema that accepts it as prose produces a green check over a claim nobody verified. ALSO FOUND: gen-skill-docs WRITES BUT DOES NOT PRUNE — `determine-intent.md` survived in the generated reference after the source was renamed. Removed by hand here; the generator keeping orphans is worth its own look. Verified: full suite 3012 pass 0 fail; tsc and eslint clean; bootstrap:graph:check, check:workflow-refs, check:declared-assets, check:declared-paths, kg:audit:check, check:bean-parents, check:agents-xref all rc=0; the new process's 6 activities all name a skill that resolves (6/6 100%).
