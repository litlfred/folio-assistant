{
  "$schema": "folio-glossary/v1",
  "id": "kg-schema-fields",
  "title": "Schema fields, extracted (large-datasets)",
  "description": "Candidate terms extracted from every schema field with a doc comment: `<Declaration>.<field>` as the label, the comment's first paragraph as the definition. Generated by folio-assistant-core/scripts/glossary-extract.ts; not curated, and never hand-edited: a person promotes a term by authoring it in a glossary of their own.",
  "source": "Schema fields of large-datasets",
  "terms": [
    {
      "id": "artifact-store.artifactstoreschema.basis",
      "prefLabel": "ArtifactStoreSchema.basis",
      "definition": "Why this store, in one sentence — it is a choice, and a choice with no reason cannot be revisited.",
      "source": "large-datasets/schemas/artifact-store.ts#ArtifactStoreSchema.basis",
      "status": "candidate"
    },
    {
      "id": "artifact-store.artifactstoreschema.cachettlseconds",
      "prefLabel": "ArtifactStoreSchema.cacheTtlSeconds",
      "definition": "What a CDN in front of this store will hold onto. Absent means unstated, never zero.",
      "source": "large-datasets/schemas/artifact-store.ts#ArtifactStoreSchema.cacheTtlSeconds",
      "status": "candidate"
    },
    {
      "id": "artifact-store.artifactstoreschema.immutableurls",
      "prefLabel": "ArtifactStoreSchema.immutableUrls",
      "definition": "Whether the store's URLs are immutable once published. `true` means a re-publish MUST use a new path; `false` means readers may be served a cached old copy for up to `cacheTtlSeconds`.",
      "source": "large-datasets/schemas/artifact-store.ts#ArtifactStoreSchema.immutableUrls",
      "status": "candidate"
    },
    {
      "id": "artifact-store.artifactstoreschema.maxartifactbytes",
      "prefLabel": "ArtifactStoreSchema.maxArtifactBytes",
      "definition": "Largest single artifact, where the host states one. Absent means unstated, never unlimited.",
      "source": "large-datasets/schemas/artifact-store.ts#ArtifactStoreSchema.maxArtifactBytes",
      "status": "candidate"
    },
    {
      "id": "artifact-store.artifactstoreschema.publicbaseurl",
      "prefLabel": "ArtifactStoreSchema.publicBaseUrl",
      "definition": "The base the artifact will be READABLE at. Required, and it is the \"EXTREME care in URL handling\" clause in schema form: a published URL is a promise, and the address has to be decided before the upload rather than discovered from whatever the host returned.",
      "source": "large-datasets/schemas/artifact-store.ts#ArtifactStoreSchema.publicBaseUrl",
      "status": "candidate"
    },
    {
      "id": "source-descriptor.identifiersystemschema.authoritative",
      "prefLabel": "IdentifierSystemSchema.authoritative",
      "definition": "Whether this identifier is guaranteed by an authority outside the source. A Handle is; a DSpace UUID is the source's own; a local slug is neither. The distinction decides which identifier survives the source moving, and IRIS is the worked case — one of its two handles points at a regional instance that was merged away.",
      "source": "large-datasets/schemas/source-descriptor.ts#IdentifierSystemSchema.authoritative",
      "status": "candidate"
    },
    {
      "id": "source-descriptor.sourcedescriptorschema.character",
      "prefLabel": "SourceDescriptorSchema.character",
      "definition": "The characterisation the \"ingest a new data source\" process produces. Required — these are what decide how the source may be REFERENCED, and a descriptor that has not said cannot answer that question.",
      "source": "large-datasets/schemas/source-descriptor.ts#SourceDescriptorSchema.character",
      "status": "candidate"
    },
    {
      "id": "source-descriptor.sourcedescriptorschema.nodekind",
      "prefLabel": "SourceDescriptorSchema.nodeKind",
      "definition": "What one node IS in this corpus. Different per source, and never assumed.",
      "source": "large-datasets/schemas/source-descriptor.ts#SourceDescriptorSchema.nodeKind",
      "status": "candidate"
    },
    {
      "id": "source-descriptor.sourcedescriptorschema.subsetbasis",
      "prefLabel": "SourceDescriptorSchema.subsetBasis",
      "definition": "Why `subsetIsSelfContained` has the value it has. Optional, because a self-contained source (IRIS: fetch an item and you have it) has little to say; the `false` case is the one that needs its reason recorded.",
      "source": "large-datasets/schemas/source-descriptor.ts#SourceDescriptorSchema.subsetBasis",
      "status": "candidate"
    },
    {
      "id": "source-descriptor.sourcedescriptorschema.subsetisselfcontained",
      "prefLabel": "SourceDescriptorSchema.subsetIsSelfContained",
      "definition": "Whether a subset chosen by the strategies above stands on its own. `false` means every request must be closed over dependencies first.",
      "source": "large-datasets/schemas/source-descriptor.ts#SourceDescriptorSchema.subsetIsSelfContained",
      "status": "candidate"
    },
    {
      "id": "source-descriptor.sourcedescriptorschema.system",
      "prefLabel": "SourceDescriptorSchema.system",
      "definition": "The software, where there is some — `DSpace 7`, `Lean 4 / Lake`. Descriptors for one system are often reusable.",
      "source": "large-datasets/schemas/source-descriptor.ts#SourceDescriptorSchema.system",
      "status": "candidate"
    },
    {
      "id": "source-descriptor.sourcedescriptorschema.termsurl",
      "prefLabel": "SourceDescriptorSchema.termsUrl",
      "definition": "Whether the source states terms, and where. Feeds the `restrictions` and `copyright` gates; `unknown` stays unknown.",
      "source": "large-datasets/schemas/source-descriptor.ts#SourceDescriptorSchema.termsUrl",
      "status": "candidate"
    }
  ]
}
