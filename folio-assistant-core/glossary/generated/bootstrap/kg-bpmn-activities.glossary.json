{
  "$schema": "folio-glossary/v1",
  "id": "kg-bpmn-activities",
  "title": "BPMN activities, extracted (bootstrap)",
  "description": "Candidate terms extracted from every BPMN task and call activity: `name` as the label, its own `<documentation>` as the definition, the element id as the code. Generated by folio-assistant-core/scripts/glossary-extract.ts; not curated, and never hand-edited: a person promotes a term by authoring it in a glossary of their own.",
  "source": "BPMN activities of bootstrap",
  "terms": [
    {
      "id": "process_discussion.a_answer",
      "prefLabel": "Answer, or decline",
      "definition": "The Requestor lane, filled by a person or by a sibling agent. Declining is a permitted move and not an error: it produces `outcome: unsettled` with what is still open, never a guess.",
      "notation": "A_Answer",
      "source": "bootstrap/processes/discussion.bpmn#A_Answer",
      "status": "candidate"
    },
    {
      "id": "process_discussion.a_narrowcandidates",
      "prefLabel": "Narrow the candidates from context",
      "definition": "Before asking. A repository whose root already holds a declaration, `<name>.json`, is not a blank slate, and a question whose candidates the agent could have worked out itself wastes the one question it is entitled to.",
      "notation": "A_NarrowCandidates",
      "source": "bootstrap/processes/discussion.bpmn#A_NarrowCandidates",
      "status": "candidate"
    },
    {
      "id": "process_discussion.a_putquestion",
      "prefLabel": "Put the question, with its candidates named",
      "definition": "Uses the `discuss` tool. One question where one will do — the Bootstrapping Agent's persona is \"asks exactly one question when it must\". A question a reader must go and research is not ready to be asked.",
      "notation": "A_PutQuestion",
      "source": "bootstrap/processes/discussion.bpmn#A_PutQuestion",
      "status": "candidate"
    },
    {
      "id": "process_discussion.a_recorddetermination",
      "prefLabel": "Record the determination",
      "definition": "Writes a document conforming to `discussion.output.schema.json`: the harness, the repositories as read-from / written-to pairs, `determinedBy`, and who answered. This artefact is what finishes the task.",
      "notation": "A_RecordDetermination",
      "source": "bootstrap/processes/discussion.bpmn#A_RecordDetermination",
      "status": "candidate"
    },
    {
      "id": "process_discussion.a_recordunsettled",
      "prefLabel": "Record what is still open",
      "definition": "The declined route. `outcome: unsettled` with `stillOpen`, so the next actor resumes rather than restarts. An agent that reaches for a default here has produced a guess, not a determination.",
      "notation": "A_RecordUnsettled",
      "source": "bootstrap/processes/discussion.bpmn#A_RecordUnsettled",
      "status": "candidate"
    },
    {
      "id": "process_initializeharness.a_confirmharness",
      "prefLabel": "Confirm WHICH harness, and where",
      "definition": "The only step needing a person, and the only place a judgement is made. IN, from the Bootstrapping Agent: a list of zero or more harnesses and a list of zero or more locations. OUT, from the Requestor: zero or ONE harness, and a list of zero or more locations. One at most — a list of two is not an answer, and the Bootstrapping Agent may not break the tie itself.",
      "notation": "A_ConfirmHarness",
      "source": "bootstrap/processes/initialize-harness.bpmn#A_ConfirmHarness",
      "status": "candidate"
    },
    {
      "id": "process_initializeharness.a_install",
      "prefLabel": "Follow them, at each location",
      "definition": "The instructions belong to the harness being installed, not to bootstrap — bootstrap does not know what any Harness above it requires, and does not need to.",
      "notation": "A_Install",
      "source": "bootstrap/processes/initialize-harness.bpmn#A_Install",
      "status": "candidate"
    },
    {
      "id": "process_initializeharness.a_listharnesses",
      "prefLabel": "List the harnesses this could be",
      "definition": "Zero or more. The DEFAULT is bootstrap itself. Context may name others: \"please set up <owner>/<repo> here\" in a discussion names one, and so does any Harness built on bootstrap that the Requestor mentions. This step narrows; it does not decide.",
      "notation": "A_ListHarnesses",
      "source": "bootstrap/processes/initialize-harness.bpmn#A_ListHarnesses",
      "status": "candidate"
    },
    {
      "id": "process_initializeharness.a_listlocations",
      "prefLabel": "List the locations this could install to",
      "definition": "Zero or more. Is this a git repository already, or were one or more URLs to repositories supplied? Both are Knowledge Graph Data Stores; the difference is only how they are reached.",
      "notation": "A_ListLocations",
      "source": "bootstrap/processes/initialize-harness.bpmn#A_ListLocations",
      "status": "candidate"
    },
    {
      "id": "process_initializeharness.a_logfailure",
      "prefLabel": "Log the failure",
      "definition": "Both failure paths pass through here, so \"logged as failed\" is a step somebody performs rather than an adjective on an end event. The end event said the process ended logged; nothing said who logged it or with what. The two ways in are the reason it is ONE call rather than two: what differs between them is the message, which is an input, not the mechanism.",
      "notation": "A_LogFailure",
      "source": "bootstrap/processes/initialize-harness.bpmn#A_LogFailure",
      "status": "candidate"
    },
    {
      "id": "process_initializeharness.a_loginstallstart",
      "prefLabel": "Log the start of the install",
      "definition": "REQUIRED logging, which is what drawing the call says. Logged before anything is written, so an install interrupted halfway is distinguishable from one never begun. A callActivity rather than a task: `log-message` is an independent sub-process, the second and last diagram in bootstrap. Any step may call it without being drawn; this one is drawn because here it is a step rather than a courtesy.",
      "notation": "A_LogInstallStart",
      "source": "bootstrap/processes/initialize-harness.bpmn#A_LogInstallStart",
      "status": "candidate"
    },
    {
      "id": "process_initializeharness.a_readdeclaration",
      "prefLabel": "Read the harness's declaration and instructions",
      "definition": "Performed against the data store — a git repository, through the git CLI or a forge API. The instructions are at a PREDETERMINED spot on the harness: `<stub>/docs/bootstrap/initialization.md`, the same for every harness, which is what lets a Bootstrapping Agent be pointed at one nobody has written yet.",
      "notation": "A_ReadDeclaration",
      "source": "bootstrap/processes/initialize-harness.bpmn#A_ReadDeclaration",
      "status": "candidate"
    },
    {
      "id": "process_initializeharness.a_readlocation",
      "prefLabel": "Read what each location already is",
      "definition": "Ask the location, not your memory of it: a Knowledge Graph Data Store that carries a declaration at its root is ALREADY an instance, and it says so itself. `<name>.json` present and parsing is the whole test, and `kg-navigation` says what the three answers mean — absent (not an instance yet, which is this process's case), present, and present-but-unreadable, which is an instance asserting something broken and is not a green light either. Before reading the chosen harness rather than after, deliberately: if the location is spoken for there is nothing to learn from the harness, and fetching it first only makes the failure more expensive.",
      "notation": "A_ReadLocation",
      "source": "bootstrap/processes/initialize-harness.bpmn#A_ReadLocation",
      "status": "candidate"
    },
    {
      "id": "process_initializeharness.a_writerootreadme",
      "prefLabel": "Write the root README, if it is not there",
      "definition": "The repository now IS an instance of something, and nothing at its root says so. A person landing on the checkout — or an agent that arrives before it has found any declaration — reads README.md first, and until this step there was no guarantee one existed. It carries two things and no more: a LINK to the harness that was installed, and the OVERALL install status across every location A_Install touched. Not one line per location buried in a log — the status a reader wants is \"is this repository set up, and as what\". WHEN ABSENT, never replacing. A repository that already has a README has one somebody wrote, and overwriting it would destroy authored content to state a fact that belongs in a generated region. Where a README is already there, the link and status go in a marker pair the harness's own readme_sync maintains. WRITING IT IS NOT A PROCESS WRITE. `instance-readme` declares `layer: context` — read at session start, never written by a running process — and this step writes one. Both hold, because INITIALISATION IS NOT PROCESS RUNTIME: the rule governs a process operating on an instance that exists, and this is the act that brings the instance into being. See the harness's content-context-and-state-graphs, which states it.",
      "notation": "A_WriteRootReadme",
      "source": "bootstrap/processes/initialize-harness.bpmn#A_WriteRootReadme",
      "status": "candidate"
    },
    {
      "id": "process_logmessage.a_compose",
      "prefLabel": "Compose the message",
      "definition": "Five required strings — timestamp, actor, process, task, message — and one optional markdown body. The five are required because each answers a question a reader of the log will otherwise have to guess, and a guessed answer in a log is worse than a missing one.",
      "notation": "A_Compose",
      "source": "bootstrap/processes/log-message.bpmn#A_Compose",
      "status": "candidate"
    },
    {
      "id": "process_logmessage.a_record",
      "prefLabel": "Record it where a person can read it",
      "definition": "Performed by the Logger, which records and decides nothing. In bootstrap the one available destination is the discussion the human actor is already in.",
      "notation": "A_Record",
      "source": "bootstrap/processes/log-message.bpmn#A_Record",
      "status": "candidate"
    }
  ]
}
