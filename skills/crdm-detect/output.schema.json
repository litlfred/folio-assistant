{
  "$id": "https://litlfred.github.io/folio-assistant/skills/crdm-detect/output.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CRDM Detect Output",
  "description": "The verdict on one request. `fires` is the decision; `categories` and `excluded` are the evidence for it, so a reviewer can see why it fired or why it did not.",
  "type": "object",
  "required": [
    "fires",
    "categories",
    "excluded"
  ],
  "properties": {
    "fires": {
      "type": "boolean",
      "description": "True when the request is flagged as a feature request: at least one detection category matched and no exclusion applied."
    },
    "categories": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "direct-capability",
          "workflow-gap",
          "platform-change",
          "cross-cutting",
          "review-surfaced",
          "self-declared-genre"
        ]
      },
      "description": "The detection categories whose signals matched — the skill's `### ` sections. Empty when none did. Two or more from different categories is high confidence."
    },
    "excluded": {
      "type": "boolean",
      "description": "True when an exclusion applied (the request matched a signal but is not a feature request, e.g. a content edit). An excluded request does not fire."
    }
  },
  "additionalProperties": false,
  "staging": {
    "branch": "main",
    "sha": "0cd7278026658b5688e18ce84da5a4fc887179f5",
    "pr": "main",
    "run": "35996084045"
  }
}
