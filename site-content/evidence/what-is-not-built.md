Everything below is **absent**, not partially working. The diagram is a model of
the intended process; these are the parts with no code behind them.

| | state |
|---|---|
| the trusted-registry list | not decided |
| observatory API bindings | not written; no endpoint verified from this container |
| the authority record's schema | not designed |
| the grading system a folio declares | not chosen |
| an `evidence-retrieval` skill | does not exist — the diagram names it, nothing implements it |
| PICO fields on a recommendation block | not in the schema |

The diagram naming a skill that does not exist is deliberate and is the same
convention the ingestion diagrams use: the model says what *would* implement a
step, and the gap between the model and the code is visible rather than hidden.
