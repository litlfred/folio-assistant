{
  "name": "large-datasets",
  "version": "0.1.0",
  "description": "Taking a usable subset of a corpus this instance will never hold, and refreshing it. Named here rather than inferred from the directory: every instance's skills live in a directory called `skills`, so a package id taken from the basename collides with every other one. See bean `nlvl` for the measurement and `bootstrap/skills` for the precedent this follows.",
  "skills": [
    "copy-out-materialized",
    "materialize-remote"
  ],
  "docker": {
    "baseImage": "ubuntu:24.04",
    "aptPackages": []
  },
  "lifecycleStages": []
}
