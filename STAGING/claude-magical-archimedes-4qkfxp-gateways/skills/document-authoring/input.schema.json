{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://litlfred.github.io/folio-assistant/STAGING/claude-magical-archimedes-4qkfxp-gateways/skills/document-authoring/input.schema.json",
  "title": "Document Authoring Input",
  "description": "Input schema for the document-authoring skill: create or revise one content block in a document folio.",
  "type": "object",
  "required": [
    "document",
    "chapter",
    "section",
    "kind",
    "label"
  ],
  "properties": {
    "document": {
      "type": "string",
      "description": "Document slug under content/."
    },
    "chapter": {
      "type": "string",
      "description": "Chapter directory name within the document."
    },
    "section": {
      "type": "string",
      "description": "Label or title of the section whose blocks[] will name this block."
    },
    "kind": {
      "type": "string",
      "enum": [
        "prose",
        "example",
        "remark",
        "algorithm",
        "simulator",
        "equation",
        "diagram",
        "table"
      ],
      "description": "Block kind. Restricted to the document profile: the seven math kinds (definition, theorem, lemma, proposition, corollary, conjecture, proof) are the paper profile and are rejected by content_profile_check."
    },
    "label": {
      "type": "string",
      "description": "Stable identifier, e.g. rec:cold-chain-audit. Becomes the block's HTML anchor; must survive renumbering."
    },
    "title": {
      "type": "string",
      "description": "Short display title."
    },
    "uses": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Labels of the blocks a reader must already have read. Direct neighbours only, never the transitive closure. Editorial judgement — never derived."
    },
    "body": {
      "type": "string",
      "description": "GitHub-flavoured Markdown for the block's .md file."
    }
  },
  "staging": {
    "branch": "claude/magical-archimedes-4qkfxp-gateways",
    "sha": "f41ad98e0fc1466caa6d3b96ea1b360bed8e6d89",
    "pr": "1051/merge",
    "run": "35857603543"
  }
}
