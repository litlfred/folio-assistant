{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://litlfred.github.io/folio-assistant/STAGING/claude-sharp-fermi-xvs06i/skills/document-structure/input.schema.json",
  "title": "Document Structure Input",
  "description": "Input schema for the document-structure skill: add, remove or reorder chapters and sections.",
  "type": "object",
  "required": [
    "document",
    "operation"
  ],
  "properties": {
    "document": {
      "type": "string",
      "description": "Document slug under content/."
    },
    "operation": {
      "type": "string",
      "enum": [
        "add-chapter",
        "add-section",
        "reorder-chapters",
        "reorder-sections"
      ],
      "description": "Structural change to make. Reordering edits the manifest only — directories are never renamed, because labels, uses[], feedback and QA sidecars all key on names."
    },
    "chapter": {
      "type": "string",
      "description": "Chapter directory name. Required for every operation except reorder-chapters."
    },
    "title": {
      "type": "string",
      "description": "Human-readable title, for the add-* operations."
    },
    "position": {
      "type": "integer",
      "description": "Zero-based index to insert at. Appends when omitted."
    },
    "order": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "The complete new order, for the reorder-* operations. Must be a permutation of the existing entries."
    }
  },
  "staging": {
    "branch": "claude/sharp-fermi-xvs06i",
    "sha": "826173352d03c1c77c1056f81ee0c348c3d660f8",
    "pr": "841/merge",
    "run": "35715128959"
  }
}
