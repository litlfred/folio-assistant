{
  "$schema": "kg-qa/v1",
  "subject": {
    "kind": "graph",
    "id": "kg",
    "path": null
  },
  "source_hash": null,
  "criteria": {
    "skill-has-entry-point": {
      "result": "pass",
      "findings": []
    },
    "consulted-skill-not-performed": {
      "result": "fail",
      "findings": [
        {
          "where": "bean-blocking",
          "detail": "\"bean-blocking\" declares `consulted: true` — reference material nobody performs — but a role carries it or an activity names it. One of the two is wrong. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        }
      ]
    },
    "skill-in-role-or-process": {
      "result": "fail",
      "findings": [
        {
          "where": "asset-extraction",
          "detail": "no role carries \"asset-extraction\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "confirm-harness",
          "detail": "no role carries \"confirm-harness\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "discussion",
          "detail": "no role carries \"discussion\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "docs-auto",
          "detail": "no role carries \"docs-auto\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "fhir-client-operations",
          "detail": "no role carries \"fhir-client-operations\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "hypothesis-generation",
          "detail": "no role carries \"hypothesis-generation\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "iris-dspace",
          "detail": "no role carries \"iris-dspace\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "log-message",
          "detail": "no role carries \"log-message\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "scientific-critical-thinking",
          "detail": "no role carries \"scientific-critical-thinking\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "scientific-visualization",
          "detail": "no role carries \"scientific-visualization\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "smart-launch",
          "detail": "no role carries \"smart-launch\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "theme-artefacts",
          "detail": "no role carries \"theme-artefacts\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "theme-contrast",
          "detail": "no role carries \"theme-contrast\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "theme-declaration",
          "detail": "no role carries \"theme-declaration\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "theme-generation",
          "detail": "no role carries \"theme-generation\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "theming",
          "detail": "no role carries \"theming\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `cat-bootstrap-render` at `cat-bootstrap/render/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`, `kg-navigation` at `kg-navigation/skills/`, `large-datasets-skills` at `large-datasets/skills/`, `who-iris-skills` at `who-iris/skills/`) — a nested instance may name it, and this audit does not read one."
        }
      ]
    },
    "manifest-skill-exists": {
      "result": "pass",
      "findings": []
    },
    "remote-skill-is-servable": {
      "result": "pass",
      "findings": []
    },
    "actor-roles-resolve": {
      "result": "pass",
      "findings": []
    },
    "actor-capabilities-resolve": {
      "result": "pass",
      "findings": []
    },
    "actor-permissions-resolve": {
      "result": "pass",
      "findings": []
    },
    "actor-is-not-a-role": {
      "result": "pass",
      "findings": []
    },
    "nested-instance-audited": {
      "result": "fail",
      "findings": [
        {
          "where": "agent-skills/harness.json",
          "detail": "nested instance \"agent-skills\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "cat-bootstrap-tools/harness.json",
          "detail": "nested instance \"cat-bootstrap-tools\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "cat-bootstrap/harness.json",
          "detail": "nested instance \"cat-bootstrap\" declares its own graph, and this audit does not read it — 3 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "detangle/harness.json",
          "detail": "nested instance \"detangle\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "folio-assistant-core/harness.json",
          "detail": "nested instance \"folio-assistant-core\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "folio-assistant-sci/harness.json",
          "detail": "nested instance \"folio-assistant-sci\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "harness.json",
          "detail": "nested instance \"folio-assistant\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "kg-navigation/harness.json",
          "detail": "nested instance \"kg-navigation\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "large-datasets/harness.json",
          "detail": "nested instance \"large-datasets\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "who-iris/harness.json",
          "detail": "nested instance \"who-iris\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "who-style-guide/harness.json",
          "detail": "nested instance \"who-style-guide\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        }
      ]
    }
  },
  "totals": {
    "pass": 7,
    "fail": 3,
    "n/a": 0,
    "unknown": 0
  }
}
