{
  "$schema": "kg-qa/v1",
  "subject": {
    "kind": "graph",
    "id": "kg",
    "path": null
  },
  "source_hash": null,
  "criteria": {
    "skill-has-entry-point": {
      "result": "pass",
      "findings": []
    },
    "consulted-skill-not-performed": {
      "result": "pass",
      "findings": []
    },
    "skill-in-role-or-process": {
      "result": "fail",
      "findings": [
        {
          "where": "confirm-harness",
          "detail": "no role carries \"confirm-harness\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "corpus-grep",
          "detail": "no role carries \"corpus-grep\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "discussion",
          "detail": "no role carries \"discussion\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "domain-fencing",
          "detail": "no role carries \"domain-fencing\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "edge-kinds-and-blast-radius",
          "detail": "no role carries \"edge-kinds-and-blast-radius\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "fhir-client-operations",
          "detail": "no role carries \"fhir-client-operations\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "fsh-guts",
          "detail": "no role carries \"fsh-guts\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "hypothesis-generation",
          "detail": "no role carries \"hypothesis-generation\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "log-message",
          "detail": "no role carries \"log-message\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "process-state",
          "detail": "no role carries \"process-state\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "scientific-critical-thinking",
          "detail": "no role carries \"scientific-critical-thinking\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "scientific-visualization",
          "detail": "no role carries \"scientific-visualization\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "smart-base-tools",
          "detail": "no role carries \"smart-base-tools\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        },
        {
          "where": "smart-launch",
          "detail": "no role carries \"smart-launch\" and no activity names it — reached, if at all, by direct invocation. In this instance's graph only (read: `cat-harness` at `skills/`, `cat-bootstrap` at `cat-bootstrap/skills/`, `methodology-raci` at `methodologies/raci/`, `methodology-crdm` at `methodologies/crdm/`, `cat-harness-src` at `src/skills/`) — a nested instance may name it, and this audit does not read one."
        }
      ]
    },
    "manifest-skill-exists": {
      "result": "pass",
      "findings": []
    },
    "remote-skill-is-servable": {
      "result": "pass",
      "findings": []
    },
    "actor-roles-resolve": {
      "result": "pass",
      "findings": []
    },
    "actor-capabilities-resolve": {
      "result": "pass",
      "findings": []
    },
    "actor-permissions-resolve": {
      "result": "pass",
      "findings": []
    },
    "actor-is-not-a-role": {
      "result": "pass",
      "findings": []
    },
    "nested-instance-audited": {
      "result": "fail",
      "findings": [
        {
          "where": "cat-bootstrap/harness.json",
          "detail": "nested instance \"cat-bootstrap\" declares its own graph, and this audit does not read it — 3 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        },
        {
          "where": "harness.json",
          "detail": "nested instance \"folio-assistant-checkout\" declares its own graph, and this audit does not read it — 0 diagram(s) there are unaudited by this run. That is correct: one instance's graph must not carry another's nodes. Audit it from its OWN root, and do NOT declare its directories here — that re-introduces the leak instance-graph-isolation.test.ts guards."
        }
      ]
    }
  },
  "totals": {
    "pass": 8,
    "fail": 2,
    "n/a": 0,
    "unknown": 0
  }
}
