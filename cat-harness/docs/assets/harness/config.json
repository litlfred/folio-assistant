---
# The Harnesses config panel's data (issue #1146), published as JSON for the
# glass. RENDERED from `site.data.harness.config`, which `harness-panel.ts`
# generates into `_data/harness.json` and `docs:harness:check` gates. One
# declaration, one generator, one gate.
layout: null
permalink: /assets/harness/config.json
sitemap: false
---
{{ site.data.harness.config | jsonify }}
