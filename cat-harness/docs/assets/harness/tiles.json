---
# The declared tiles, published as JSON for pages Jekyll does not build.
#
# A who-iris replica page carries no `<meta name="fa-tiles">` — no Jekyll
# wrote it — so the folio glass fetches this instead. It is RENDERED from the
# same `site.data.harness.tiles` the meta is, so there is one declaration and
# two ways it reaches a page, never two lists. Bean `zrvt`, issue #1006.
layout: null
permalink: /assets/harness/tiles.json
sitemap: false
---
{{ site.data.harness.tiles | jsonify }}
