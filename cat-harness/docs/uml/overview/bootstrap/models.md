---
layout: default
title: "UML — bootstrap/models"
nav_exclude: true
---

# UML — bootstrap/models

Generated by `cat-harness/scripts/gen-uml-overview.ts` — do not edit. The `models` sub-graph of `bootstrap` (`bootstrap/models`), with every attribute read from its node schema.

**Sources (same model):** [PlantUML](https://github.com/litlfred/folio-assistant/blob/main/cat-harness/uml/overview/bootstrap/models.puml) · [Mermaid](https://github.com/litlfred/folio-assistant/blob/main/cat-harness/uml/overview/bootstrap/models.mmd)

<div class="fa-uml-views">
  <fieldset class="fa-uml-view-pick">
    <legend>Layout</legend>
    <input type="radio" name="fa-uml-overview_bootstrap_models" id="fa-uml-overview_bootstrap_models-portrait" class="fa-uml-pick-portrait" checked><label for="fa-uml-overview_bootstrap_models-portrait">Portrait</label>
    <input type="radio" name="fa-uml-overview_bootstrap_models" id="fa-uml-overview_bootstrap_models-landscape" class="fa-uml-pick-landscape"><label for="fa-uml-overview_bootstrap_models-landscape">Landscape</label>
  </fieldset>
  <figure class="bpmn-figure fa-uml-portrait">
    <img src="{{ '/assets/img/uml/overview/bootstrap/models.svg' | relative_url }}" alt="UML class diagram of bootstrap/models: one package per named sub-graph, one class per node schema, with its data fields. Portrait layout.">
  </figure>
  <figure class="bpmn-figure fa-uml-landscape">
    <img src="{{ '/assets/img/uml/overview/bootstrap/models.landscape.svg' | relative_url }}" alt="UML class diagram of bootstrap/models: one package per named sub-graph, one class per node schema, with its data fields. Landscape layout.">
  </figure>
</div>

| sub-graph | directory | graph kinds | node schema | nodes | cohesion | in | out |
|---|---|---|---|---|---|---|---|
| `bootstrap/models` | `bootstrap/models` | models | `ModelRegistrySchema` | *not measured* | | | |

*Nodes, cohesion, in and out* are the detangler's pinned measurements for the same directory (`bun run kg:detangle`; skill [`graph-detanglement`](https://github.com/litlfred/folio-assistant/blob/main/cat-harness/skills/graph-management/graph-detanglement.md)). *Not measured* means the detangler does not scan that directory, not that it has no edges.

## Sub-graphs

- [← all of bootstrap](../bootstrap.html)

## The same model, drawn by Mermaid

Kept so the page renders even where the PlantUML image is missing, and because Mermaid nodes carry the CSS class that colours them from `uml.css`.

```mermaid
classDiagram
  direction TB
  namespace bootstrap__models {
    class bootstrap_models_Model["Model"] {
      <<json: ModelRegistrySchema>>
      id [1] string
      title [1] string
      preferredLanguages [0..*] string[]
      validation [1] unverified | self-reported | human-validated
      validatedBy [0..1] string
      validatedOn [0..1] string
      note [0..1] string
    }
    class bootstrap_models_ModelRegistry["ModelRegistry"] {
      <<json: ModelRegistrySchema>>
      $schema [1] 'folio-model-registry/v1'
      models [0..*] Model[]
    }
  }
  bootstrap_models_ModelRegistry *-- "0..*" bootstrap_models_Model : models
  cssClass "bootstrap_models_Model" fa_uml_kind_models
  cssClass "bootstrap_models_ModelRegistry" fa_uml_kind_models
```
