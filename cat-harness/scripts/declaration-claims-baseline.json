{
  "_comment": "Bean vzur, 2026-09-21: EMPTY, and that is the finished state rather than a disabled one. It shipped with bean hrv2 carrying four entries -- the two skills that named `harness.json` as the declaration filename, plus the two GENERATED copies under docs/reference/skill-instructions/ that follow them. All four are gone because the backlog they stood for was cleared: the skills now say `<name>.json`, and the generated pair was REGENERATED rather than hand-edited. Keep the file: a NEW contradiction fails the check whether or not anything is listed here, and an entry that stops matching is reported stale, so this list shrinks rather than fossilises. Adding an entry is admitting a known-wrong claim, so it wants a bean and a reason.",
  "known": []
}
