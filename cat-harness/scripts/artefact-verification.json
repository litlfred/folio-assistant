{
  "_comment": "Per generated-artefact check: what verifies the artefact for its CONSUMER, as opposed to verifying that the committed copy is current. The two are different questions and the difference has cost six pages — library:viz:check was green while the page it generated could not run (PR #805). The check list is DERIVED from package.json and never listed here. Undeclared is a finding: 'nobody has said' is not 'nothing to check'. A `none` entry needs a REASON; an empty one looks decided and is not. This file may only SHRINK as entries move from `none` to `verified`; a declaration for a check that no longer exists is reported stale.",
  "verified": {
    "docs:auto:check": "generated-viewer-scripts.test.ts — compiles every inline script in every generated viewer via `new Function`, which parses without executing. Derived over the whole generated tree, so a new viewer family is covered the day it is generated. This is the check that PR #805 proved was missing: library:viz:check was green while the page could not run.",
    "external-schemas:viz:check": "external-schemas-viz.test.ts, and the half that matters runs over the REAL registry: every `](#id)` link in the summary table is resolved against the `<a id>` anchors the page carries, in BOTH the freshly rendered string and the committed file, so a record renamed or dropped fails here and not in a reader's browser (`pb04`). It also walks EVERY table on the page and pins each row's column count — `operative` is free prose across 53 terms and an unescaped pipe shifts every column to its right, rendering a table that is subtly wrong rather than obviously broken — and refuses to pass if that scan found fewer than ten rows, so a generator that stopped emitting tables cannot read as clean. The fixture half pins `classifyUsedBy`'s three states, including that a deliberate glob and a genuinely missing path stay distinguishable.",
    "folio:viz:check": "folio-three-states.e2e.ts §\"the folio viewer page RUNS\" — six specs that LOAD the generated page in a browser and drive it against three projection states, which is the question a staleness check structurally cannot answer. It asserts no pageerror fires, that the row count equals the nodes in the real committed projection, and that the badge agrees with the rows it shows (tis1: a badge also makes a WRONG count visible). The three states are asserted apart because they are opposite facts the page must not render alike: a graph with nodes, an EMPTY graph (\"not a graph that could not be read\"), and a projection that 404s (\"not an empty folio\"). Plus folio-viz.test.ts, which asserts against the EMITTED string: the mount is present when the caller passes one and absent when it does not, the declared coverage.visualiser resolves to a file that exists (a declared path resolving to nothing is pb04 aimed at a declaration), and NO BACKTICK reaches the page — bmr0, which caught this author the same day in gen-library-viz. Written because this gate's own comment names the precedent: library:viz:check was green while the page it generated could not run (PR #805).",
    "glossary:check": "glossary-export.test.ts, and it runs buildGlossary() over the REAL corpus rather than a fixture, so its assertions are about the emitted document: every live concept carries a prefLabel and no two share an @id; a concept's @id is the one kg-export already mints for that role, so the glossary does not fork the IRI space; notation is unique; every `usage` link resolves to a node in the same document; and the scheme is the document itself rather than a separate `…#scheme` IRI that would not dereference (`blv9`). The ledger half is covered separately — retirement, un-retirement, firstSeen preserved across a rebuild, and a wrong $schema REFUSED rather than overwritten. NOT verified: nothing dereferences the @context or runs a real JSON-LD processor over the output — the graph is JSON-LD by convention rather than by construction, which is bean `vigi`.",
    "kg:export:check": "qa-results.test.ts §\"the result and the document are two renderings of ONE computation\" — parses the COMMITTED `test/results/kg-export.qa-results.json`, asserts its `$schema` is `qa-results/v1`, and cross-checks every family's count against `buildExport`'s own field, so a drift between what the exporter computes and what was last written is caught rather than assumed. checkQaResult uses the SAME comparison key as writeQaResult (`withoutTimestamp`), so the verifier and the writer cannot disagree about what unchanged means — the `nytj` family, where a writer and a reader with different keys are each correct alone. FOUR states, not two: `missing` and `unreadable` are kept apart from `stale`, because a generator that never ran and one whose output moved need different answers. WHAT IS NOT VERIFIED, said rather than implied: the exported document `_kg/<stub>.jsonld` is a gitignored BUILD OUTPUT with no committed copy, so `--check` compares only the sidecar; checking the document would compare a build to itself. Bean `v556`.",
    "kg:locale:check": "kg-locale-export.test.ts, and the coverage is UNEVEN in a way worth stating. translateDocument's assertions run over a fixture, not the real graph: a translated value carries its own @language tag, an untranslated string falls through plain and untagged, the document declares the SOURCE language rather than the locale, every @id stays the core's, identity-bearing keys and links are never rewritten even on a msgid hit, and the core document is not mutated. Only three tests run over the real graph — that there are locales to report on, that every locale is reported whether emitted or not, and localeDocumentsUnreferenced()'s four banned shapes over the committed core, which is the one consumer-facing invariant checked against what is actually shipped. The mutation that proves the IRI-protection test is not vacuous is in the fixture set. NOT verified: the same @context gap as glossary:check (`vigi`), and no check that a committed per-locale document's @ids still resolve after the core moves.",
    "library:viz:check": "generated-viewer-scripts.test.ts — compiles every inline script in every generated viewer via `new Function`, which parses without executing. Derived over the whole generated tree, so a new viewer family is covered the day it is generated. This is the check that PR #805 proved was missing: library:viz:check was green while the page could not run.",
    "methodologies:viz:check": "methodologies-viz.test.ts, and the half that matters runs over the REAL corpus rather than a fixture: every `](#name)` link in the page's selection table is resolved against the `<a id>` anchors the page itself carries, in BOTH the freshly rendered string and the committed file — so a link to a section that was renamed or dropped fails here, not in a reader's browser (`pb04`). It also pins the column count of every table row, because `origin` and `applies-when` are free prose and one of them already contains a pipe: an unescaped one shifts every column to its right and renders a table that is subtly WRONG rather than obviously broken. The fixture half pins the join — which of the three evidence states a node lands in, keyed by node PATH rather than by name, since two instances may adopt methods one letter apart.",
    "navbar:geometry:check": "navbar.test.ts §\"the geometry is stated ONCE — `sjic`\" — seven specs asserting the CONSUMER-facing fact rather than the currency of the committed copy. The load-bearing one converts the generated `rem` back to px and asserts it equals `navbar-geometry.ts`'s own constant for collapsed/open/mark: the question the artefact exists to answer, which a byte comparison cannot ask. A second asserts no definition of any `--fa-nav-*` appears OUTSIDE the generated region while all appear inside it, so the gate fails if somebody re-adds a local copy rather than only if the generator drifts. Three more cover the region mechanics: docs-ui.css is self-sufficient (region present, and ahead of every rule reading it), regeneration is idempotent rather than stacking, and an UNTERMINATED region is REFUSED rather than guessed — a wrong guess eats authored CSS, the one outcome a generator over a hand-written file must never have. WHY DECLARED RATHER THAN SEEDED: the defect it closes was invisible to a suite that had a test on each side — `navbar.ts` asserted its own numbers and `sidebar-strip.test.ts` asserted `docs-ui.css`'s, both green while the rail rested at 40px and the sidebar at 56px. A staleness check alone would reproduce exactly that. THE REGION SHAPE IS ITSELF A FIX, measured: generating a SEPARATE navbar-geometry.css left docs-ui.css depending on a file that 23 e2e fixtures inline it without, and an undefined custom property is invalid at computed-value time and therefore SILENT — sidebars took auto width and a11y clicks timed out against a button that was visible and stable throughout. NOT verified: nothing renders the two surfaces side by side and compares laid-out widths. They were measured in Chromium by hand at 1280 and 1000 — 56/264 and 56/248 — but that is in no gate, so a THEME change overriding these values would not be caught here.",
    "processes:viz:check": "processes-viz.test.ts — asserts the page's CLAIMS, not its currency. Every SVG it names exists on disk; every diagram it reports as unrendered is verified absent (the check that would have caught 11 phantom findings); a diagram that will not load is present in the output rather than dropped; the skill→process join is complete over every skill any activity names; and the census of skill-less activities is asserted NOT to read as a defect list, because its first version accused 31 deliberate design decisions. Also a vacuity guard: the generator refuses to write an index over zero diagrams, after its first run reported success over a corpus of 62. NOTE THE SHAPE, offered on PR #876: the validity is enforced on every run — `bun test` is itself a gate — but it is NOT enforced by the `:check` script, which is a byte comparison like the other 42. `validated, but not by the check` may deserve to be its own state rather than folded into `verified`.",
    "schema:viz:check": "generated-viewer-scripts.test.ts — compiles every inline script in every generated viewer via `new Function`, which parses without executing. Derived over the whole generated tree, so a new viewer family is covered the day it is generated. This is the check that PR #805 proved was missing: library:viz:check was green while the page could not run.",
    "uploads:viz:check": "generated-viewer-scripts.test.ts — compiles every inline script in every generated viewer via `new Function`, which parses without executing. Derived over the whole generated tree, so a new viewer family is covered the day it is generated. This is the check that PR #805 proved was missing: library:viz:check was green while the page could not run.",
    "voices:viz:check": "generated-viewer-scripts.test.ts — compiles every inline script in every generated viewer via `new Function`, which parses without executing. Derived over the whole generated tree, so a new viewer family is covered the day it is generated. This is the check that PR #805 proved was missing: library:viz:check was green while the page could not run."
  },
  "none": {
    "agent-memory:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "avatars:css:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "boards:default:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "bootstrap-tools:schemas:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "check:raci": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "check:subgraphs": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "check:theme-art:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "check:undeclared-files:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "deps:python:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "docs:harness:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "external-schemas:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "fsh-guts:viz:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "gen:jsonld:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "handler:index:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "harness:dirs:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "ingest:ig:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "iris:covers:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "iris:pages:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "kg:audit:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "kg:detangle:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "kg:schema:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "landing:data:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "landing:sticky:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "ns:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "readme:sync:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "readme:sync:root:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "render:bpmn:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "smart-trust:pages:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "state:visualizer:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "themes:css:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "tools:viz:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "translate-bpmn:bootstrap:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "translate-bpmn:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "translate-kg-viewer:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "translation:block-qa:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "translation:index:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does.",
    "translation:status:check": "NOT YET ASSESSED — seeded by --write-baseline so the gate fails on a NEW artefact kind rather than on the backlog. Replace with what verifies this artefact for its consumer, or with the reason nothing does."
  }
}
