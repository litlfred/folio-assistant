{
  "name": "qou-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Paper writing assistant MCP server — rendering, validation, Lean, preview",
  "scripts": {
    "start": "bun run server.ts",
    "start:stdio": "bun run server.ts --stdio",
    "start:http": "bun run server.ts --http",
    "check-deps": "bun run deps.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@unified-latex/unified-latex-util-parse": "^1.8.0",
    "@unified-latex/unified-latex-util-print-raw": "^1.8.0",
    "zod": "^3.23.0"
  }
}
