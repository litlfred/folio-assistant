{
  "_comment": "The WHO SMART Immunizations artefact index — the SECOND ingested IG, and the one that tested whether the pipeline generalises. Declared exactly as `smart-trust` is on main today, MINUS a `docs` graph: the owner ruled on 2026-09-21 that generated HTML is not wanted ('i dont want the. /html generated... just the calculated versions, dependcies and such the AST end'), so this instance ships the index and its DAK surface and renders nothing. PROVISIONAL BY DESIGN: the owner has also ruled that a per-IG harness should not exist at all ('there does not neeed to be smart-trust harness, b/c no new funcionality'), with the DAK/IG pipeline belonging in an IG base and the output landing in a `smart-guideline` page under a new `smart-base` harness (bean nsbb). This declaration keeps the repository CONSISTENT rather than half-migrated while that restructure is decided; it is not an argument that the instance should persist.",
  "name": "smart-immunizations",
  "description": "WHO SMART Immunizations (smart.who.int.immunizations) v0.2.0, FHIR 4.0.1 — the published IG's artefact index, reconstructed: 748 artefacts known, 200 materialized.",
  "directories": [
    {
      "id": "smart-immunizations-artifact-index",
      "path": "fhir-artifact-index/",
      "dependents": "skip",
      "description": "The reconstructed artefact index and the DAK API surface it points at. 40.6 MB of sidecars, materialised on the owner's explicit choice of 'everything' over the 11.3 MB schema+jsonld subset — 133x smart-trust's 332 KB, because this IG publishes 198 schemas and 188 display files against smart-trust's 19 and 14. `index.json` carries a `provenance` block naming the published file every field came out of, and `dakUnbound` records any sidecar an enumeration listed that bound to no artefact.",
      "graphKinds": [
        "fhir-artifact-index"
      ],
      "coverage": {
        "skill": "ig-artifact-ingestion"
      },
      "readOnly": true,
      "readOnlyBasis": "Copies of upstream IG artefacts, and GENERATED -- `ingest-ig-artifacts.ts` rewrites this index, a person does not edit it. Read-only is about the second: a hand edit here silently changes what this repository claims upstream published, and `check:materialized-fixity` is what catches one that happened."
    }
  ],
  "assets": [
    {
      "id": "agent-instructions",
      "src": "AGENTS.md",
      "role": "agent-instructions",
      "title": "AGENTS.md — smart-immunizations",
      "description": "Cold start for this layer, and the standing caveat that the instance is provisional pending the smart-base restructure."
    },
    {
      "id": "instance-readme",
      "src": "README.md",
      "role": "instance-readme",
      "title": "smart-immunizations",
      "description": "What this index is, what it was reconstructed from, and what the second IG proved about the pipeline."
    }
  ],
  "needs": [
    "folio-assistant-core"
  ]
}
