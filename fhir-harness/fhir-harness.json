{
  "_comment": "THE BARE FHIR IG LAYER, staged as a top-level directory on the path who-iris, smart-trust, smart-immunizations and smart-base are already on. Created 2026-09-22 on the owner's stack ruling: `core->fhir-harness-> smart-base->siblings{smart-l1, smart-dak, smart-ig}`, no smart-guidelines. It is bean `nsbb`'s 'base', now named: run the FHIR IG Publisher with NO pre-processing and NO post-processing and publish the result, which is how SMART Guidelines were built BEFORE the DAK phases were added -- a shape that demonstrably worked rather than one being invented. IT MUST STAY WHO-FREE. The package here may be referenced BY `authoring-who-smart-guidelines` and may never reference it; a violation fails nothing, the layer simply stops being usable by the non-WHO IG it exists for, which is why `ig-build-pipeline` states the refusal as a checkable list rather than as an intention. DECLARES ONLY `skills/`, because that is all that exists: a declared-but-absent directory is the `dh4f` defect, where every consumer scans nothing and reports a clean run over it.",
  "name": "fhir-harness",
  "_title_comment": "The HUMAN display name, for the LHS navbar's harness tabs (bean `t3n8`). `name` stays the machine identifier that filenames and `needs` edges resolve against.",
  "title": "FHIR IG Harness",
  "description": "The bare FHIR IG pipeline — SUSHI, the IG Publisher, Jekyll assembly and publication — with no WHO, DAK or SMART assumption. What a non-WHO implementation guide instantiates.",
  "directories": [
    {
      "id": "fhir-ig-skills",
      "path": "skills/",
      "dependents": "reproduce",
      "description": "Two packages. `fhir-client` is SMART on FHIR app launch (`smart-launch`) and FHIR resource operations (`fhir-client-operations`) through the SMARTerFHIR library, authored here against a pinned upstream commit because that repository ships no skill files (issue #556, bean `wlqd`). `fhir-ig-base` is the IG pipeline. `ig-build-pipeline` states what the layer runs and the list of things it refuses to know about, including the deploy phase's assignment. `ig-render-jekyll` states the three render contracts -- JSON only, navigation derived from `sushi-config.yaml`'s own ordered `pages:` and `menu:` maps rather than authored, and the LHS rail with the IG theme captured rather than discarded. `ig-publisher-reduction` is the five-phase transition to an AST-only Publisher, each phase carrying an exit criterion that is a measurement or a diff rather than an impression. `ig-publisher-fork` is the brief for an agent taking a local experimental fork, and says why a publisher-only fork cannot satisfy its central criterion: the Library/PlanDefinition/Measure edges are produced in `org.hl7.fhir.core`, not in the publisher.",
      "graphKinds": [
        "skills"
      ],
      "coverage": {
        "skill": "smart-stack-layering"
      }
    },
    {
      "id": "fhir-ig-tools",
      "path": "tools/",
      "dependents": "skip",
      "description": "The two `Library` strippers, moved down from the WHO build's \"DAK Postprocessing\" phase because neither is DAK-shaped -- any IG depending on `hl7.fhir.uv.cql` produces oversized `Library` resources. Declared here rather than only described in a skill: a layer boundary that exists in prose is one no consumer can act on, and these were described as belonging here for a whole round while being declared nowhere.",
      "graphKinds": [
        "tools"
      ],
      "coverage": {
        "skill": "ig-build-pipeline"
      }
    }
  ],
  "assets": [
    {
      "id": "agent-instructions",
      "src": "AGENTS.md",
      "role": "agent-instructions",
      "title": "AGENTS.md — fhir-harness",
      "description": "Cold start for work in this layer, and its one rule: nothing here may know about WHO."
    },
    {
      "id": "instance-readme",
      "src": "README.md",
      "role": "instance-readme",
      "title": "fhir-harness",
      "description": "What the layer is, what it deliberately excludes, and which two steps came down into it from the WHO build."
    }
  ],
  "_needs_comment": "It sits directly on core. NOT on cat-harness: the harness layer is inherited through core rather than declared twice, in the same shape folio-assistant-core uses. Owner's ordering, 2026-09-20: bootstrap, cat-harness, f-a-core, then the layers above.",
  "needs": [
    "folio-assistant-core"
  ],
  "_canonicalUrl_comment": "A placeholder base under this repository's own publication root, NOT a FHIR canonical: this layer publishes no FHIR artefacts, it publishes a Tool graph. It exists because a Tool node's I/O type IRIs are minted against it at load and Zod refuses a relative one. Stated rather than left blank so nobody reads it as a claim that fhir-harness is an IG.",
  "canonicalUrl": "https://litlfred.github.io/folio-assistant/fhir-harness",
  "_name_comment": "THE NAME IS NOT AN INSTANTIATION CLAIM. `fhir-harness` is DECLARED here and is not instantiated: instantiation is a `<name>.config.json` at the REPOSITORY root (`harness-tiles`, bean `6n23`), and there is no `fhir-harness.config.json`. It also declares no `coverage.visualiser` on either directory, so by the tile rule it owes no tile and gets none — making it, measured 2026-09-23, the least harness-like thing in this repository that carries the word in its name. That is not a defect: this repository stages future instances as siblings, and `description` above says so (\"What a non-WHO implementation guide instantiates\") — a downstream IG instantiates it, this checkout does not. Written down because the name says otherwise to anyone who has not read the declaration, and `check:harness-claims` reports the pairing rather than leaving the next reader to re-derive it."
}
