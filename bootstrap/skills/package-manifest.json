{
  "name": "bootstrap",
  "version": "0.1.0",
  "description": "What an agent reads before it knows whether this repository is an instance, what kind, or what for. The coldest path in the tree: every other skill assumes an answer these provide. Named by bootstrap/workflows/initialize-harness.bpmn and bootstrap/workflows/discussion.bpmn, which the ROOT audit deliberately does not read — one instance's graph must not carry another's nodes — so this manifest is what makes the skills servable rather than merely present.",
  "skills": [
    "bootstrap-graph-emission",
    "bootstrap-graph-publication",
    "bootstrap-kg-navigation",
    "confirm-harness",
    "discussion",
    "log-message",
    "root-readme"
  ],
  "docker": {
    "baseImage": "ubuntu:24.04",
    "aptPackages": []
  },
  "lifecycleStages": []
}
