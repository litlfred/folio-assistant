{
  "name": "bootstrap",
  "version": "0.1.0",
  "description": "What an agent reads before it knows whether this repository is an instance, what kind, or what for. The coldest path in the tree: every other skill assumes an answer these two provide. Named by bootstrap/workflows/initialize-harness.bpmn, which the ROOT audit deliberately does not read \u2014 one instance's graph must not carry another's nodes \u2014 so this manifest is what makes the skills servable rather than merely present.",
  "skills": [
    "confirm-harness",
    "kg-navigation"
  ],
  "docker": {
    "baseImage": "ubuntu:24.04",
    "aptPackages": []
  },
  "lifecycleStages": []
}
