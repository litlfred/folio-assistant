{
  "$schema": "folio-dublin-core/v1",
  "id": "local:who-pub-tps-931",
  "_comment": "DERIVED, NOT TRANSCRIBED -- there is no IRIS capture for this item. THE PDF'S OWN EXTRACTED METADATA IS WRONG AND THAT IS THE POINT: structure.json carries `title: \"Abies\"` and `authors_raw: \"Cpe N\"`, which is OCR noise off a scanned title page (121 pages, text_source `ocr`, the only scanned item of the three). A pipeline that trusted derived metadata would have catalogued this as a publication called 'Abies'. The title and date below are read from the document itself by a human/agent; every one of them must be REPLACED by the IRIS record when iris.who.int is reachable.",
  "fields": [
    {
      "schema": "dc",
      "element": "title",
      "values": [
        {
          "value": "WHO Editorial Style Manual",
          "language": "en"
        }
      ]
    },
    {
      "schema": "dc",
      "element": "date",
      "qualifier": "issued",
      "values": [
        {
          "value": "1993"
        }
      ]
    },
    {
      "schema": "dc",
      "element": "publisher",
      "values": [
        {
          "value": "World Health Organization",
          "language": "en"
        }
      ]
    },
    {
      "schema": "dc",
      "element": "language",
      "qualifier": "iso",
      "values": [
        {
          "value": "en"
        }
      ]
    },
    {
      "schema": "dc",
      "element": "format",
      "qualifier": "extent",
      "values": [
        {
          "value": "121 p."
        }
      ]
    }
  ],
  "provenance": {
    "source": "cat-harness/library/who-pub-tps-931/structure.json",
    "retrievedAt": "2026-09-20",
    "method": "DERIVED from the ingested PDF and read by hand, NOT transcribed from an IRIS record. No DSpace UUID, handle or collection path is known for this item and iris.who.int is egress-blocked from this environment (CONNECT tunnel failed, 403). Replace wholesale when it is reachable."
  }
}
