{
  "id": "pandoc",
  "name": "Pandoc",
  "description": "Universal document converter. The document adapter's whole render path goes through it: Markdown → HTML and Markdown → PDF, neither of which needs a TeX installation.",
  "detection": { "method": "command", "command": "pandoc --version" }
}
