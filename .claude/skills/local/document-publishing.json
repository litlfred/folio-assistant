{
  "id": "document-publishing",
  "name": "Document Publishing",
  "description": "Render a document folio to Markdown, HTML and PDF without a TeX installation. The PDF path drives pandoc with an HTML engine and never falls back to latexmk, so a folio that claims to need no TeX keeps that true on a clean machine.",
  "roles": ["author", "publication-manager", "admin"],
  "requiredCapabilities": [
    { "capabilityId": "pandoc", "degradation": "fail" },
    { "capabilityId": "html-pdf-engine", "degradation": "warn" }
  ],
  "routingPatterns": [
    "render the document", "publish", "export pdf", "export html",
    "build the document"
  ],
  "tags": ["document", "publishing", "pandoc"],
  "package": "authoring-document",
  "lifecycleStages": ["publish", "test"],
  "schemaRef": "schemas/skills/document-publishing"
}
