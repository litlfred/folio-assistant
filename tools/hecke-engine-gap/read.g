#############################################################################
##  read.g — HeckeEngine package read script.
##
##  Run after init.g; loads the GAP-language implementations that
##  delegate to the kernel functions.

ReadPackage( "HeckeEngine", "lib/HeckeEngine.gi" );
